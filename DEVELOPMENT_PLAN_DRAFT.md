# 本地 AI PPT 生成软件开发方案（已确认的详细参考）

> 文档状态：方向已于 2026-08-23 确认；具体模板与解析规则仍需逐阶段审核。  
> 日期：2026-08-23  
> 目标平台：Windows 本地桌面软件，不建设 Web 前端、服务端或在线账号系统。  
> 首个业务场景：工业自动化 / 机器视觉设备技术方案 PPT。

## 1. 结论先行

本项目建议做成一套本地“项目资料 → 结构化数据 → 业务模块 → 认证模板页面 → 可编辑 PPTX”的桌面工具。

第一版的核心不是让 AI 自由设计 PPT，而是先建立稳定的模板自动化能力：

```text
用户建立项目
  ↓
选择已认证的 PPT 模板
  ↓
导入 Excel / Word / 图片
  ↓
解析并让用户确认结构化数据
  ↓
选择或由 AI 建议业务模块
  ↓
模块映射到模板中的认证页面和可替换位置
  ↓
替换文字、图片、表格、图表
  ↓
结构与视觉检查
  ↓
输出不覆盖原模板、可在 PowerPoint 中继续编辑的 PPTX
```

推荐原则：

- AI 决定“写什么、选哪些模块、推荐哪个 Layout”。
- 模板和渲染器决定“坐标、字体、颜色、大小、对齐和排版”。
- 任意 AI 结果必须先成为结构化数据，不能直接改 OOXML 或随意生成坐标。
- 原模板永远只读；每次生成新文件和新版本。

## 2. 对当前想法的独立评判

### 2.1 合理且应保留的方向

- 做 Windows 本地桌面软件，符合当前个人/公司内部使用场景，也减少部署和运维成本。
- 以现有公司 PPT 模板为视觉基准，比让 AI 自由排版可靠得多。
- Excel、Word、图片分别解析后再用于 PPT，方向正确。
- 把 PPT 拆成公司介绍、需求、设备方案、模块介绍、参数等业务模块，是整个系统最重要的数据结构。
- 输出必须是可编辑 PPTX，而不是整页截图或 PDF。

### 2.2 必须纠正的三个假设

1. **不能承诺任意 PPT 模板导入后就能自动修改。**

   真实模板中的对象命名、组合、图表、图片关系和母版用法差异很大。第一版只能稳定支持“认证模板”：先识别页面、标注模块、标注可替换对象，再允许自动生成。

2. **Excel / Word 不能解析后直接灌入 PPT。**

   两者必须先进入“数据确认页”。用户要能看到解析出的客户、产品、检测项、参数、流程、图片等字段，修正后才能生成 PPT。否则一旦表格列名或 Word 标题变化，错误会直接进入交付文件。

3. **26 个已选功能不能全部作为第一版。**

   预览、单页重生成、AI 选 Layout、Word/PDF 泛化解析、溢出/重叠检测都依赖稳定的底层模型。一次做完会让问题互相掩盖，无法判断是模板、解析、AI 还是渲染出错。

### 2.3 本方案新增的必要能力

以下不是额外堆功能，而是让所选功能可实现的基础设施：

- 模板认证与人工标注工具。
- 模块、Layout、Slot 三层定义。
- 导入数据确认与字段映射。
- 项目版本、模板版本和输出版本管理。
- 生成日志、错误报告和原文件保护。
- 图片去重、分类、标签和来源记录。

## 3. 已读取的功能选择

选择表中删除了 10 项，保留了 26 项。本方案将“仍保留的行”解释为认可功能：

| 分类 | 保留编号 | 内容概括 |
|---|---|---|
| AI 内容 | A1–A6 | 大纲、页面模块、Layout、页面内容、容量约束、Word/PDF 等资料提取 |
| 模板与生成 | B1、B2、B3、B6、B7、B8、B10 | 选模板、复制页、替换对象、可编辑 PPTX、原生表格/图表、结构检查 |
| 图片和图示 | C1、C2 | 插入用户图片、文字生成流程图 |
| 本地交互 | D1–D8 | 一键流程、大纲修改、模块勾选、预览、单页重生成、编辑、项目保存、AI 配置 |
| 质量检查 | E1–E3 | 文字溢出、元素重叠、图片分辨率/拉伸检查 |

用户最新口头需求新增：

- **本地图片素材库**：公司图、技术方案图、产品图、设备图、案例图等分类。

原清单中的 C5 虽被删除，但最新口头需求优先，因此重新纳入。第一版采用人工分类和标签；AI 自动分类放到后续。

## 4. 当前模板的可行性检查

已只读检查：`templates/冲压筒形壳体检测方案NAT6704_v2.pptx`。

> 该文件只用于早期可行性理解，不作为正式开发模板。用户将在进入模板阶段时另行提供并审核具体模板。

| 项目 | 结果 |
|---|---:|
| 文件大小 | 35,356,063 字节 |
| 页面 | 23 页 |
| Master | 1 个 |
| Layout | 11 个 |
| 媒体文件 | 26 个 |
| 幻灯片尺寸 | 13.33 × 7.5 英寸（16:9） |
| PPTX 包结构 | 正常 |

模板具备作为首个试点的条件，但还不是“可自动调用模板”：

- 整份文件存在 20 个 Placeholder，但 23 页均没有被 `python-pptx` 识别出的标准标题占位符。
- 大量对象名是“文本框 1”“图片 1”“Rounded Rectangle 11”等通用名称，并有重复。
- 复杂流程页包含大量独立形状和连接线，不能依赖模糊文字匹配修改。
- 表格、图片、组合对象必须分别验证替换后是否保留样式和关系。

建议先把这 23 页人工认证成以下业务模块：

| 试点模块 | 当前页 | 用途 |
|---|---:|---|
| `cover` | 1 | 封面、项目名称、编号、日期、联系人 |
| `company_intro` | 2、21 | 公司介绍、公司资质 |
| `customer_requirement` | 3 | 产品和检测需求、特别说明、样品图 |
| `inspection_flow` | 4 | 设备检测流程 |
| `equipment_solution` | 5–9 | 整机、上料、搬运、下料等设备模块 |
| `equipment_parameters` | 10 | 设备参数表 |
| `inspection_result` | 11–16 | 检测效果、检测项说明 |
| `vision_system` | 17–19 | 机器视觉、工控机、AI 算法 |
| `core_components` | 20 | 主要部件品牌 |
| `revision_record` | 22 | 版本记录 |
| `ending` | 23 | 结束页 |

这只是第一次映射，最终名称和必选/可选规则需要用户审核。

## 5. 必须统一的四个概念

### 5.1 业务模块 Module

表达“这一部分讲什么”，例如公司介绍、客户需求、设备总体方案、上料机构、检测流程、设备参数。

一个业务模块可以对应 1～N 页。

### 5.2 页面版式 Layout

表达“这一页如何呈现”，例如左图右文、参数表、流程图、四图展示。

同一个模块可以有多个候选 Layout，但第一版每个模块先认证 1～3 个，不追求 10 个。

### 5.3 可填槽位 Slot

表达“页面中的哪个对象能填什么”，例如：

```text
title
summary
product_image
equipment_image
parameter_table
inspection_items
caption
```

每个 Slot 要记录对象名称或对象 ID、数据类型、文字容量、图片比例、是否必填。

### 5.4 项目数据 ProjectData

Excel、Word、人工输入和 AI 输出最终都转成同一份结构化项目数据；PPT 渲染器只读取 ProjectData，不直接读取任意原始文档。

## 6. 产品功能设计

### 6.1 主界面

建议采用左侧步骤导航，避免做成复杂 PowerPoint 编辑器：

1. 项目信息
2. 资料导入
3. 数据确认
4. 图片素材
5. 模板选择
6. 模块与大纲
7. 页面内容
8. 生成与检查

### 6.2 模板中心

- 导入 PPTX 模板，但不修改原文件。
- 读取页数、母版、Layout、对象、图片、表格等信息。
- 给每页指定业务模块。
- 给可修改对象设置 Slot 名称和容量规则。
- 保存 `template_manifest.json`，不把规则硬编码在程序里。
- 生成缩略图并做人工确认。
- 通过兼容性测试后标记为“已认证”；未认证模板不能正式生成。

### 6.3 Excel 导入

第一版只支持约定模板或用户确认过的字段映射，不做“任意 Excel 智能识别”。

流程：

```text
选择 Excel
  ↓
识别工作表、表头、单元格、图片
  ↓
选择解析配置
  ↓
显示解析结果和缺失项
  ↓
用户修正/确认
  ↓
写入 ProjectData
```

先支持高价值数据：客户与项目、产品信息、检测要求、设备参数、检测项、主要部件。

### 6.4 Word 导入

第一版支持有标题层级、明确表格和图片的 DOCX：

- 标题 → 候选模块或段落。
- 正文 → 内容块。
- 表格 → 结构化表格。
- 图片 → 素材候选，并记录来自哪个文档和段落。

不在第一版承诺任意 Word 长文自动准确拆分。PDF 解析比 Word 更不稳定，放到后续里程碑。

### 6.5 图片素材库

图片文件保存在项目或素材目录，SQLite 只存元数据，不把大图片直接塞进数据库。

第一版字段：

- 分类：公司、资质、产品、设备、机械模块、技术方案、检测效果、案例、其他。
- 标签：客户、产品型号、设备型号、工位、视角、版本。
- 来源、版权/使用说明、原始路径、文件哈希、尺寸、比例、导入时间。
- 重复图片提示和低分辨率提示。

第一版人工选择分类；后续 AI 只提供分类建议，用户确认后保存。

### 6.6 模块与大纲

- 软件根据 PPT 类型加载模块清单。
- 用户勾选需要的模块，并拖动调整顺序。
- AI 可以根据项目资料推荐模块和大纲，但必须由用户确认。
- 模块选择后，只显示模板中已认证的候选页面。
- 每个模块可单独编辑标题、正文、图片、表格和备注。

### 6.7 生成与修改

- 从原模板复制需要的页面，或从认证模板副本删除不需要的页面。
- 按 Slot 精确替换文字、图片、表格和图表。
- 内容超过容量时先提示；后续再实现自动缩短、换 Layout 或拆页。
- 允许重新生成某一模块/页面，但最终仍输出一个新 PPTX。
- 每次生成保存清单：模板版本、输入资料、模块、AI 模型、生成时间和检查结果。

## 7. 推荐技术架构

```text
PySide6 Windows 桌面界面
        │
        ├── Project Workspace（项目、版本、设置）
        ├── Import Adapters
        │     ├── Excel：openpyxl
        │     ├── Word：python-docx
        │     └── Image：Pillow + 文件哈希
        ├── ProjectData / Module / Layout / Slot 模型
        ├── Template Registry + template_manifest.json
        ├── AI Provider Adapter（可关闭）
        ├── Renderer Adapter
        │     ├── python-pptx：分析、简单替换、结构验证
        │     └── PptxGenJS + pptx-automizer：模板页复制和原生对象生成
        ├── Validation
        └── SQLite + 本地文件目录
```

### 技术选择判断

- **桌面 UI：PySide6**。与现有 Python 代码、Office 解析库集成成本最低；Qt 官方提供 Windows 桌面部署工具。正式分发前必须完成 LGPL/GPL/商业许可证合规评估。
- **本地存储：SQLite + 文件目录**。不是服务端数据库，不需要启动端口或守护进程。
- **Python 继续作为主程序**。现有原型和 Office 解析能力可以保留。
- **PPTX 渲染采用适配器，而不是一开始写死单一库**。
- **PptxGenJS / pptx-automizer 以本地命令行子进程调用**，不是后端服务，不监听网络端口。
- **正式选定渲染路径前，必须用当前 35MB 模板做技术 Spike**。如果纯 Python 能稳定满足认证模板，就先减少 Node 打包复杂度；如果页面复制、图表或关系保真失败，再启用 Node 双引擎。
- **桌面打包优先评估 Qt 官方 `pyside6-deploy`**，它面向 Windows、macOS、Linux 并基于 Nuitka；不要在开发第一周就花时间做安装包。

## 8. AI 功能边界

第一版 AI 只允许输出经过 Schema 校验的内容：

- 项目摘要。
- PPT 大纲。
- 推荐业务模块。
- 推荐候选 Layout ID。
- 页面标题、正文、要点。
- 流程图 Mermaid 文本。
- 对已有文字按容量进行压缩或改写。

AI 不允许：

- 直接生成或修改 x/y/w/h 坐标。
- 直接写 OOXML。
- 未经确认删除模板页或原文件。
- 把未经核实的技术参数写成确定事实。
- 未经用户确认自动使用图片分类或来源不明的图片。

AI 模型通过 Provider Adapter 配置。第一版建议先支持一个 OpenAI-compatible API 接口；是否必须完全离线，需要用户确认。API Key 不写入项目文件，正式实现时使用 Windows 凭据存储或等价安全方案。

## 9. 分阶段开发计划

以下工期是假设 1 名开发者、有真实模板和输入样例、用户能及时审核的粗估，不是承诺日期。

### 阶段 0：模板与渲染技术 Spike（3～5 个工作日）

目标：在开发 GUI 和 AI 前，证明当前模板能被稳定修改。

工作：

- 建立当前 23 页的模块映射草案。
- 选 5 种代表页：封面、图文、复杂流程、参数表、图片展示。
- 对标题、正文、图片、表格做精确替换。
- 比较纯 `python-pptx` 和本地 Node 渲染方案的保真度、打包复杂度。
- 输出新 PPTX，不覆盖模板。
- PowerPoint 打开无修复提示，关键对象可编辑。

退出条件：

- 5 种页面全部通过结构和人工视觉检查。
- 明确选定第一版 Renderer 路径。
- 形成 `template_manifest.json` v0.1。

### 阶段 1：确定性桌面 MVP（约 2～3 周）

覆盖：B1、B2、B3、B6、B10、C1、D1、D3、D7，以及新增的基础素材库。

功能：

- 新建、保存、打开本地项目。
- 选择已认证 PPT 模板。
- 导入图片、人工分类和标签。
- 勾选模块、调整模块顺序。
- 为 Slot 选择文字或图片。
- 生成可编辑 PPTX。
- 包结构、页数、媒体关系和原文件保护检查。

退出条件：

- 用户不写代码即可完成一次“选模板 → 选模块 → 选图片 → 生成”。
- 连续生成 20 次不损坏模板、不覆盖历史输出。
- PowerPoint 打开无修复提示。

### 阶段 2：Excel / Word 数据工作台（约 2～3 周）

覆盖：A6 的 Excel/Word 部分、B7、B8、D2。

功能：

- 支持首批 1～2 种真实 Excel 模板解析。
- 支持首批 1 种结构化 Word 模板解析。
- 数据确认、缺失项提示、人工修改和字段映射。
- 从 ProjectData 填写原生 PPT 表格。
- 生成简单原生图表；复杂图表另做后续验证。
- 生成前查看并修改大纲和页面内容。

退出条件：

- 同一份数据可重复生成一致结果。
- 修改 Excel/Word 中一个关键值，重新解析后能追踪到对应 PPT Slot。
- 无法解析的字段明确显示，不静默丢失。

### 阶段 3：AI 辅助生成（约 2 周）

覆盖：A1–A5、C2、D8。

功能：

- 配置 AI Provider。
- AI 生成大纲和页面内容。
- AI 推荐模块和候选 Layout。
- 根据 Slot 容量限制文字长度和要点数量。
- 文本生成 Mermaid 流程图。
- 所有 AI 结果先进入确认界面，再进入 PPT。

退出条件：

- AI 输出 100% 通过 JSON Schema 或给出可理解的重试错误。
- AI 关闭时，确定性生成链路仍可使用。
- 同一项目可保存 AI 输入、模型配置摘要和用户最终确认内容。

### 阶段 4：预览、单页修改和质量检查（约 2～3 周）

覆盖：D4–D6、E1–E3，以及 A6 的 PDF 部分。

功能：

- 页面缩略图预览。
- 单页或单模块重新生成。
- 修改页面文字后重新输出整份 PPTX。
- 检查文字溢出、对象重叠、图片分辨率和拉伸。
- 评估 PDF 文本、表格和图片抽取；达不到可靠度时只作为辅助资料来源。

退出条件：

- 单页修改不会破坏其他页面和模板关系。
- 质量问题能定位到页码、对象和建议动作。
- 预览失败不阻止生成，并显示替代检查方式。

### 阶段 5：打包与内部试用（约 1 周）

- 固定依赖版本和 Python 版本。
- 构建 Windows 安装包或便携目录。
- 在无开发环境的 Windows 电脑验证。
- 检查 Qt、Node 依赖及第三方许可证。
- 准备备份、升级和错误日志导出。

## 10. 第一版明确不做

- 不做 Web 前端、后端服务、用户账号、云端项目库或多人协同。
- 不做完整 PowerPoint 画布编辑器。
- 不支持任意 PPTX 自动变成高质量模板库。
- 不支持任意 Excel/Word/PDF 无配置准确解析。
- 不让 AI 自由控制坐标或直接操作 OOXML。
- 不用整页 HTML 截图作为正式 PPTX 输出。
- 不在第一版做 AI 生成背景图、自动网络搜图、AI 视觉评审和全自动失败重试。
- 不在未做许可证评估前对外分发软件。

## 11. 核心数据对象

| 对象 | 作用 |
|---|---|
| `Project` | 一个客户/设备方案项目及其版本 |
| `SourceDocument` | 导入的 Excel、Word、PDF、图片及来源 |
| `ProjectData` | 用户确认后的统一结构化数据 |
| `Asset` | 图片、SVG、图表数据及其分类、标签、版权信息 |
| `TemplateVersion` | 不可变的原始 PPTX 和校验值 |
| `ModuleDefinition` | 公司介绍、设备方案、参数等业务模块 |
| `LayoutDefinition` | 模块可用的认证页面版式 |
| `SlotBinding` | 模板对象与业务字段的精确绑定 |
| `DeckPlan` | 模块顺序、页面选择和内容草案 |
| `RenderJob` | 一次生成的输入、版本、状态和日志 |
| `ValidationResult` | 结构、视觉和业务检查结果 |

## 12. MVP 验收标准

1. 原模板和原始导入资料始终不被覆盖。
2. 用户可以新建项目、保存退出、重新打开并继续操作。
3. 至少 1 个认证模板、8 个业务模块和 10 个认证页面可用。
4. 至少支持 1 种真实 Excel 模板、1 种结构化 Word 模板和本地图片导入。
5. 用户能在生成前确认结构化数据、模块和页面内容。
6. 输出 PPTX 在 Microsoft PowerPoint 中打开无修复提示。
7. 标题、正文、图片、表格等关键对象保持可编辑。
8. 生成结果包含模板版本、数据来源、模块和检查报告。
9. AI 关闭或 API 不可用时，手工/规则驱动生成仍能工作。
10. 对同一项目连续生成 20 次，不出现文件损坏、输出覆盖或关系丢失。

## 13. 开始开发前必须提供的样例

阶段 0 必需：

- 当前 PPT 模板的最终可测试版本。
- 标出其中哪些页属于哪些业务模块。
- 指定至少 5 页需要被自动替换的具体内容。

阶段 2 必需：

- 1～2 份真实 Excel 模板及手工确认的期望字段。
- 1 份真实 Word 模板及期望拆分结果。
- 每种资料至少一份正常样例和一份缺字段样例。

素材库必需：

- 20～50 张可用于测试的公司、产品、设备、方案和检测效果图片。
- 图片分类规则和不能混用的边界。

## 14. 已确认的关键决定

1. 第一版只支持 Windows 10/11。
2. 输出需要兼容 Microsoft PowerPoint 和 WPS，并在两端验收。
3. 允许联网调用 AI API。
4. 软件仅公司内部使用，不对外销售。
5. 接受新模板第一次由人工配置模块、页面和可替换位置，配置后重复使用。
6. 当前目录中的 PPTX 不作为正式模板；用户后续提供具体模板并单独审核。
7. 第一版只做工业设备技术方案 PPT。
8. Excel、Word 和图片样例在进入对应阶段前提供。
9. PySide6 作为第一版 UI，但业务核心与 UI 解耦，以保留未来更换界面的能力。

## 15. 下一步执行顺序

1. 以 `PROJECT_FRAMEWORK.md` 作为简明架构基线。
2. 以 `PROJECT_LOG.md` 记录每次工作、验证结果和下一步。
3. 等待用户提供正式 PPT 模板、Excel/Word 样例和图片素材。
4. 样例确认后编写下一阶段 Spec，只实施模板与渲染技术 Spike。
5. 提交生成结果和 PowerPoint/WPS 双端验证，由用户再次审核后才进入完整桌面 GUI 开发。

## 16. 参考资料

- 本地调研报告：`output/AI_PPT生成平台开源项目与技术路线调研报告.md`
- 本地功能选择表：`output/本地AI_PPT生成软件功能选择清单.xlsx`
- [Qt for Python 官方文档](https://doc.qt.io/qtforpython-6/)
- [Qt for Python 官方部署说明](https://doc.qt.io/qtforpython-6/deployment/index.html)
- [pyside6-deploy 官方说明](https://doc.qt.io/qtforpython-6/deployment/deployment-pyside6-deploy.html)
- [Qt for Python 商业使用说明](https://doc.qt.io/qtforpython-6/commercial/index.html)
