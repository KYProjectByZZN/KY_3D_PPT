# AI PPT 桌面软件项目框架

> 状态：2026-08-23 已确认的简明基线。详细说明见 `DEVELOPMENT_PLAN_DRAFT.md`。

## 1. 产品边界

- Windows 10/11 本地桌面软件。
- 第一版只做工业设备技术方案 PPT。
- 使用 PySide6 作为桌面界面，但业务逻辑不能写进界面层。
- 允许联网调用 AI API；AI 不可用时仍能手工/规则生成。
- 输出可编辑 `.pptx`，同时兼容 Microsoft PowerPoint 和 WPS。
- 软件仅公司内部使用。

## 2. 用户操作流程

```text
新建项目
→ 导入 Excel / Word / 图片
→ 检查和修改解析数据
→ 选择已配置的 PPT 模板
→ 组装技术方案模块和内部页面并调整顺序
→ AI 辅助生成大纲和内容
→ 人工确认
→ 生成可编辑 PPTX
→ PowerPoint / WPS 检查
```

## 3. 功能模块

| 功能 | 职责 |
|---|---|
| 桌面界面 | 项目操作、资料导入、数据确认、模块选择、生成与检查 |
| 项目管理 | 保存项目、输入资料、版本、生成记录和错误日志 |
| Excel 导入 | 通用映射；统一光学 FAR 可一键提取检测标准、工位配置、节拍和内嵌检测图 |
| Word 导入 | 提取标题、正文、表格和图片，交给用户确认 |
| 图片素材 | 导入、去重、人工分类、标签、分辨率检查 |
| 模板配置 | 人工标注页面模块、可替换文字、图片、表格和容量 |
| 方案模块 | 模块/页面增删复制排序、页面模板、结构化字段和Excel动态扩页 |
| 设备方案 | 整机图、流程节点、工位动作、实体设备模块和关联关系；同步生成流程/总览/模块页面 |
| 客户需求管理 | 永久保留原始需求，维护固定分类配置、需求记录、复制/归档和版本快照 |
| 自动候选方案 | 检索结构化历史摘要，生成候选工艺/工位、DrawingSpecification 和二维方案图 Prompt，人工确认后再进入后续审核 |
| 无CAD方案实验室 | 标准设备模块组成单条产品主线；整机与各模块分别绑定人工结构、提示词、控制图、采用图片和来源记录，通过门禁及人工确认后显式同步到正式设备方案 |
| AI 辅助 | 大纲、模块建议、Layout 建议、页面文字和流程图 |
| PPTX 生成 | 复制模板页面并精确替换内容，不覆盖原模板 |
| 质量检查 | PPTX 结构、文字溢出、对象重叠、图片质量和双端兼容 |

## 4. 简单技术分层

```text
PySide6 UI
    ↓
Application（操作流程）
    ↓
Core（ProjectData / Module / Layout / Slot）
    ↓
Adapters
    ├── Excel / Word / Image
    ├── AI Provider
    ├── Template Registry
    ├── PPTX Renderer
    └── PowerPoint / WPS Validation
    ↓
SQLite + 本地文件目录
```

原则：

- UI 只负责显示和收集操作。
- Excel、Word、图片和 AI 输出都先转成统一 `ProjectData`。
- 模板第一次人工配置，之后重复使用。
- AI 只生成结构化内容，不生成坐标、不直接写 OOXML。
- Renderer 可以替换，不把核心写死在单一 PPT 库上。
- 每个功能模块独立拥有自己的数据模型、业务服务、界面适配和针对性测试，修改时优先限制在模块内部。
- 模块联动统一经过 Application 层，用稳定 ID、明确的数据对象或事件传递；禁止界面控件互相直接调用，也禁止跨模块访问内部实现。
- 一份业务状态只允许一个权威拥有者，联动采用单向同步，避免同一数据多处保存和双向回调链。
- 只有出现真实的第二个复用方后才提取共享接口，避免为“模块化”提前建立无业务价值的复杂抽象。

## 5. 计划中的代码目录

目录按功能逐步创建，不提前建立空架子：

```text
ppt_generator/
  core/          项目数据、模块、Layout、Slot
  importers/     Excel、Word、图片解析
  templates/     模板配置与版本
  assets/        图片素材元数据
  ai/            AI Provider 与 Schema
  renderers/     PPTX 生成适配器
  validation/    结构与兼容检查
  storage/       SQLite 和项目文件
  ui/            PySide6 界面
```

## 6. 开发顺序

1. 获取并审核正式 PPT 模板及替换目标。
2. 验证模板配置和 PPTX 生成路线。
3. 做最小桌面流程：项目、模板、模块、图片、生成。
4. 加 Excel / Word 解析和数据确认。
5. 加 AI 大纲、模块、Layout 和内容辅助。
6. 加预览、单页重生成、质量检查和打包。

任何阶段没有通过验收，不提前进入下一阶段。

## 7. AI 图片接入与模块绑定（0.9.7）

- 默认采用本机 Codex 运行时和 ChatGPT 登录，使用用户已有的 Codex 会员套餐额度，不把 ChatGPT 会员误当成 API Key，也不要求 API 充值。
- Codex Provider 与原 OpenAI API Provider 通过同一 `ImageProvider` 边界接入；API 路径仅作为用户主动选择的单独计费备用模式。
- 一个 `EquipmentScene` 确定性生成一个整机视觉目标和每个节点各一个模块视觉目标；目标分别拥有结构快照、补充要求、最终提示词、目标哈希和控制SVG。
- 只允许通过逻辑门禁的Scene目标进入任一图片Provider；提交前再次计算`sceneHash/targetHash`，结构或提示词变化后旧采用图失效。
- 整机控制SVG表达主线关系，模块控制SVG只表达当前模块及上下游接口；AI只生成候选外观，不负责决定模块、顺序或连接关系。
- Codex 登录由官方浏览器流程完成，软件不读取密码或登录令牌；每次 `codex exec` 使用临时会话和独立批次目录的 `workspace-write` 沙箱。
- API 备用模式的 Key 仍只从 `OPENAI_API_KEY` 或界面本次会话输入获取，不写进项目文件、Scene、日志或生成记录。
- 每次生成保存独立批次目录、目标ID/类型/哈希、请求参数、控制图/候选图哈希、Provider、模型和请求/线程ID；人工采用后只回写对应Scene目标。
- 用户显式点击同步后，由`scheme_application`把整机及模块的结构、提示词、图片和来源记录单向导入正式设备方案；不会自动生成PPT。正式设备方案仍通过既有人工同步按钮写入设备总览和设备模块Slot。
- 无CAD编辑器提供独立大尺寸“模块效果总览”；按当前视觉目标顺序显示整机和每个模块的采用图，卡片只加载缩略图，点击后才加载原图并支持缩放。该界面只读，不保存第二份模块状态，也不合成PPT图片。
- 整机/模块生成目标在“AI设备方案效果图”窗口内选择；切换目标同步更新提示词和已有采用图，并清空上一目标候选。一个窗口可依次采用多个目标，关闭后按目标ID/哈希统一回写，外层结构编辑选择只作为初始定位。
- `PptProject` schema v5 保存稳定项目ID、无CAD Scene和AI候选批次索引；候选文件位于 `%LOCALAPPDATA%\KY_Project\PPT_Generator\data\projects\<projectId>\ai_candidates\...`，不同项目不共用历史。AI窗口仅显示项目ID、目标ID和目标哈希均匹配的批次；已保存项目生成新批次后自动原子更新项目文件。
