# 项目跟踪记录

> 用途：记录已确认决策、当前状态、每次工作结果、验证和下一步。后续开发任务完成后追加记录，不覆盖历史。

## 当前状态

- 当前实现：0.9.5 PySide6 模块化编辑 MVP + 设备方案/FAR/两级预览 + 可编辑顶部导航 + 自动方案 v2 + 无CAD整机/模块绑定、AI窗内目标选择与大尺寸效果总览 + Codex Pro/API 双图片 Provider。
- 当前阶段：逻辑通过的无CAD `EquipmentScene` 默认提交到本机 Codex 的 ChatGPT 登录路径，使用会员套餐额度；支持后台登录检查、隔离生成、候选预览、可追踪记录和人工采用。原 OpenAI API 仅保留为显式备用，AI 结果仍与正式候选方案及 PPT 隔离。
- 下一阶段：用户通过“模块效果总览”审核整机和各模块已采用图片，再继续补齐模块图并检查 ImageGen 是否保持模块数量、顺序、检测与剔除逻辑；确认后再做局部重绘和 Scene 矢量标注叠加。
- 尚未开始：真实历史/标准模块库导入、工程规则执行器、候选方案合并正式PPT、连续页面缩略图、可编辑 Office 画布、自动模板配置、安装包发布。

## 已确认决策

| 日期 | 决策 |
|---|---|
| 2026-08-23 | 第一版只支持 Windows 10/11 |
| 2026-08-23 | PySide6 作为可替换的桌面 UI，核心业务与 UI 解耦 |
| 2026-08-23 | 第一版只做工业设备技术方案 PPT |
| 2026-08-23 | 新模板第一次由人工配置，配置后重复使用 |
| 2026-08-23 | 允许联网调用 AI API；无 AI 时确定性生成仍可工作 |
| 2026-08-23 | 输出必须兼容 Microsoft PowerPoint 和 WPS |
| 2026-08-23 | 软件仅公司内部使用，不对外销售 |
| 2026-08-23 | 具体 PPT、Excel、Word 和图片样例后续逐阶段提供并审核 |
| 2026-08-23 | 页面模块先采用默认 JSON 配置，后续由软件调整，Renderer 不写死模块归属 |
| 2026-08-23 | 设备流程节点与实体设备模块分开建模，通过稳定ID关联；AI只做建议，人工确认后再同步到PPT |
| 2026-08-24 | 各功能按有边界的模块开发；跨模块联动由Application层通过稳定ID和明确数据契约统一协调，禁止界面或模块内部互相直接调用；共享状态只保留一个权威来源 |
| 2026-08-24 | 自动方案 v2 先完成客户需求管理与候选方案；原始需求不被解析器覆盖，解析建议必须人工确认，候选方案暂不合并正式PPT |
| 2026-08-24 | 方案图生成先以独立实验室核验；相同 DrawingSpecification 必须产生相同 LayoutPlan/SVG/PromptRecipe，确认前不接图片API、正式候选方案和PPT |
| 2026-08-24 | 无CAD阶段优先保证设备工艺逻辑，不追求接近真实外观；AI只接收通过门禁的EquipmentScene，内置通用模块待用户确认后再替换为公司模块 |
| 2026-08-25 | 内部软件默认通过 Codex 的 ChatGPT 登录使用会员套餐额度；OpenAI API Key 改为用户主动选择的单独计费备用路径 |

## 里程碑

| 里程碑 | 状态 | 完成条件 |
|---|---|---|
| M0 调研与功能选择 | 已完成 | 调研报告和 26 项功能选择形成 |
| M1 方案与项目框架 | 已完成 | 开发方案、简明框架和跟踪记录确认 |
| M2 模板/渲染验证 | 进行中（自动验证已过） | NAT6704 代表页面生成并通过 PPT/WPS 人工检查 |
| M3 确定性桌面 MVP | 进行中（首版已实现） | 用户审核项目、模板、模块、资料、图片和PPTX生成闭环 |
| M4 Excel/Word 工作台 | 进行中（真实光学FAR闭环已实现） | 真实样例解析、确认和字段映射闭环 |
| M5 AI 辅助 | 未开始 | 大纲、模块、Layout、内容均可确认和回放 |
| M6 预览、质检、打包 | 进行中（当前页预览已实现） | 双端兼容、质量检查和内部安装包通过 |

## 待用户提供

- NAT6704 正式项目文字、参数表、检测项表和替换图片（当前使用测试数据）。
- 用户在 Microsoft PowerPoint 与 WPS 中打开测试输出并反馈视觉问题；本机当前未检测到 WPS。
- 第一批正式 Word 模板与期望字段。
- 测试用公司、产品、设备、方案和检测效果图片。

## 工作记录

### 2026-08-23｜确认方案并建立跟踪基线

- 范围：根据功能选择和用户确认，建立简明项目框架与持续跟踪记录。
- 结果：确认 PySide6 可替换 UI、人工模板配置、技术方案单一场景、在线 AI API、PPT/WPS 双端兼容。
- 文件：`PROJECT_FRAMEWORK.md`、`PROJECT_LOG.md`、`README-AI.md`、`AGENTS.md`。
- 验证：检查文档链接、关键决策和里程碑状态；本次未修改运行代码。
- 下一步：用户提供正式模板后，先编写模板/渲染技术验证 Spec，再开始实现。

### 2026-08-23｜接收 NAT6704 候选模板并建立 Spike Spec

- 输入：`templates/冲压筒形壳体检测方案NAT6704_v2.pptx`。
- 只读结果：23 页、1 个 Master、11 个 Layout、189 个对象、22 张图片、4 个表格、14 页备注，无宏和嵌入对象。
- 原文件哈希：`795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`。
- 结果：建立 `SPEC_TEMPLATE_SPIKE.md` 和 `templates/NAT6704_v2_TEMPLATE_CONFIG_DRAFT.md`。
- 验证：PPTX ZIP 结构可读；本次未修改原模板，尚未执行 PowerPoint/WPS 双端打开测试。
- 下一步：用户确认页面处理和首批替换内容后，再开始 Renderer 技术验证。

### 2026-08-23｜完成 NAT6704 配置驱动渲染最小闭环

- 范围：按用户授权采用默认模块，模块和 Slot 全部配置化；实现文字、等尺寸表格和图片替换，不做 PySide6、资料解析或 AI。
- 实现：新增 `ppt_generator/template_renderer.py`、`render_template.py`、`templates/NAT6704_v2.template.json`、示例数据和自动化测试。
- 输出：`output/NAT6704_v2_M2配置渲染测试.pptx`，基于 6 个代表页替换测试文字及表格，未覆盖原模板。
- 自动验证：7 项单元测试通过；PPTX ZIP 无损坏；23 页、14 页备注、1 个母版、11 个 Layout、25 个媒体文件和外部网站链接保留；第 4 页仍为 33 个对象和 3 条连接线；非目标 Shape 无语义变化。
- 稳定性：同一配置连续生成 20 次通过，耗时 25.56 秒，原模板 SHA-256 保持不变。
- 双端状态：Microsoft PowerPoint 已安装，但自动无窗口打开被 Office 启动提示阻塞；本机未检测到 WPS。两端无修复提示和视觉一致性仍需人工验收。
- 下一步：用户打开测试输出检查版式；确认后开始 M3 最小桌面界面，把模板模块和 Slot 配置接入可调整的 UI。

### 2026-08-23｜实现 PySide6 桌面 MVP

- 用户纠偏：交付目标是可视化桌面软件，而不只是命令行渲染；界面参考实际 DCE 项目 `D:\test\KY_Project\ky_dce_projetct`。
- 界面：新增顶部项目操作栏、左右分屏、项目内容/方案模块/Excel-Word/图片素材四个页签、结构预览、生成检查和运行日志；采用 DCE 类似的 Steel Blue 工业风格。
- 功能：模板/配置选择，文字/表格/图片 Slot 编辑，模块启用与拖动排序，Excel/Word 通用解析，图片分类与 Slot 分配，项目保存/打开，后台生成和结果打开。
- 核心：新增 UI 无关的 `PptProject`、资料解析器；Renderer 支持按模块删页和排序，命令行默认行为不变。
- 验证：13 项单元测试通过；Windows 原生界面截图中文显示正常；真实 NAT6704 模板仅保留封面、设备参数、结束页后生成 3 页，顺序正确、ZIP 无损坏、原模板哈希不变。
- 产物：`output/ui_desktop_mvp_windows.png`、`output/NAT6704_v2_UI模块验证.pptx`。
- 下一步：用户运行 `启动PPT软件.bat` 审核交互；提供真实 Excel、Word 和图片后建立自动字段映射规则。

### 2026-08-23｜实现无样例通用 Excel 映射工作台

- 用户决定：不等待真实 Excel 样例，先完成通用功能，后续统一修改业务规则。
- 界面：Excel页签新增工作表选择、单元格预览、表头建议、连续区域选择、映射规则表、规则启用/删除/保存/加载和应用到项目。
- 映射：单元格或矩形区域可写入PPT文字；与模板尺寸一致的区域可写入PPT表格；同一目标不允许多条启用规则。
- 自动建议：扫描“字段名—右侧值”结构，按模板字段名保守匹配；候选默认不启用，必须人工确认。
- 持久化：Excel路径和映射规则随 `*.kyppt.json` 保存，另支持独立规则JSON。
- 演示：生成 `examples/excel_mapping_demo.xlsx` 和 `examples/excel_mapping_demo.json`；5条规则成功写入封面、编号、日期、7×4设备参数表和9×5检测项表。
- 验证：17项测试和Python编译通过；`output/NAT6704_v2_Excel映射验证.pptx` 为有效23页PPTX；原模板哈希保持不变；Windows原生截图 `output/ui_excel_mapping_windows.png` 中文和布局正常。
- 已知边界：未支持旧版 `.xls`、密码文件、公式刷新和Excel内嵌图片；自动业务语义需真实样例后校准。
- 下一步：用户实际操作映射流程并统一提出界面/字段修改意见。

### 2026-08-23｜模块化编辑 MVP 方案待审核

- 输入：`output/模块提示词.txt`，要求模块/页面增删改排、复制、Excel 字段绑定、按数据行动态扩页、模板与数据分离。
- 现状判断：现有“方案模块”只有模板模块的启用和排序，没有项目级 Module/Slide 实例，不能支持独立副本和动态页数。
- 方案：新增 schema v2 项目实例模型和模块服务；保留模板 manifest v1 与旧 Renderer 入口；模块/页面从模板蓝图创建，按项目实例导出。
- 边界：第一版编辑 Slot/变量，不做 Office 自由画布；Excel 重新应用仅替换该绑定生成的模块副本；图表、SmartArt、视频、OLE 和宏不在首批复制承诺内。
- 文件：新增 `SPEC_MODULAR_EDITOR_MVP.md`，并加入 AI 读取顺序。
- 只读核对：NAT6704 为 23 页，含 85 个普通图形、55 个文本框、22 张图片、20 个占位符、4 个表格和 3 条线，无图表；页面关系还包含 Notes、图片、超链接和 Tags。
- 验证：本次只完成设计分析和文档记录，未修改运行代码或模板。
- 下一步：用户确认方案后开始实现第 1 步“模型与 v1 项目迁移”。

### 2026-08-23｜补充页面模板与结构重算方案

- 用户补充：模块增加页不能盲目复制当前页；每个模块需要默认或自定义的页面模板，并处理增删页后的页数、小标题和整体逻辑。
- 方案修订：每个模块增加页面模板库、默认页面序列和默认新增页面模板；Excel 每条记录重复完整默认序列，人工增加页可选择其他模板。
- 结构规则：项目页保存稳定 UUID、页面模板 key、标题和小标题；总页数、内容页码、模块序号、模块内页码均由 SlidePlan 统一重算，不依赖原始页码。
- 模板边界：第一版自定义页面模板从当前 PPT 模板已有页面登记；外部 PPTX 页面因母版和主题合并风险暂不直接导入。
- 页码边界：封面和结束页默认不参与内容编号；只有模板配置了系统 Slot 才写入页码、模块标题和页面小标题，不在固定坐标强行添加对象。
- 文件：已修订 `SPEC_MODULAR_EDITOR_MVP.md`；本次仍未修改运行代码或模板。
- 下一步：等待用户确认页面模板和编号默认策略后开始编码。

### 2026-08-23｜明确模块内部结构化内容

- 用户纠偏：页数统计本身简单，重点是模块内部对应页面的图片、标题、小标题等内容需要结构化管理；用户意见作为方向参考，由软件方案补全。
- 设计判断：不把所有页面写死成统一字段，而由每个页面模板声明自己的 Slot Schema；参数页、图文页、缺陷页可以拥有不同结构。
- 数据层级：项目公共数据 → 模块数据 → 页面人工覆盖；字段绑定稳定模块/页面 ID 和 Slot key，不绑定页码。
- 界面：左侧只展示模块和页面；选中页面后，右侧按基本信息、文字、图片、表格和数据绑定分组编辑，避免树形结构过度膨胀。
- 图片：图片 Slot 可以声明公司图、产品图、技术方案图等素材分类，选择图片时优先过滤对应类别。
- 文件：已将结构化内容方案和验收条件补充到 `SPEC_MODULAR_EDITOR_MVP.md`；仍未修改运行代码。

### 2026-08-23｜实现 0.4.0 模块化生成与编辑 MVP

- 用户确认：按 `SPEC_MODULAR_EDITOR_MVP.md` 实施模块内部结构化管理方案。
- 项目模型：项目文件升级为 schema v2；新增 Module、Slide、PageTemplateRef 和 ExcelModuleBinding；读取 schema v1 后在模板加载时自动迁移，原项目公共数据保留。
- 模块服务：实现模块/页面添加、删除、复制、移动、页面模板登记、默认新增模板、稳定 UUID 和深复制；复制后文字、图片和表格数据互不影响。
- 结构数据：统一计算实际页数、内容页码、模块序号、模块内页码、模块标题、页面标题和小标题；封面/结束页默认不参与内容编号。
- Excel 动态扩展：选择源模块、Excel工作表、表头和字段映射；每行复制源模块当前实际页面结构；重新应用只替换该绑定旧副本，源模块作为关闭的数据模板保留。
- 界面：原模块 QListWidget 替换为“模块→页面”树；提供增删复制上下移动、页面模板登记、页面属性、按组 Slot 编辑和 Excel生成入口；右侧继续显示最终启用结构。
- Renderer：新增 `render_project()`，按项目实例复制模板页和关系图，支持文字、表格、图片、普通图形、线条、Notes、超链接和 Tags；保留旧 `render_template()`。
- 真实模板兼容：NAT6704 的 Layout 存在反向 Slide 关系，`python-pptx` 会保留未引用旧页；已增加受控清理，只移除未被 presentation 关系引用的 Slide XML 和内容类型声明，并补回归测试。
- 自动验证：23项测试通过；Python编译通过；schema v1迁移、深复制、页码重算、Excel行物化、反向关系清理和UI交互均有测试覆盖。
- 真实输出：`output/NAT6704_v2_模块结构化验证.pptx` 为24页、15份Notes、24个Slide XML；`output/NAT6704_v2_Excel模块重复验证.pptx` 为25页、3个Excel模块副本；两者ZIP无损坏且可由`python-pptx`重开。
- 原模板：SHA-256仍为 `795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`。
- 界面检查：Windows原生截图 `output/ui_module_editor_v040_windows.png` 中文、模块树、字段表和整体布局正常。
- 尚未完成：PowerPoint/WPS人工打开、无修复提示、视觉一致性与另存编辑验收；外部PPT页面导入、Office自由画布和AI仍不在本阶段。

### 2026-08-23｜实现 0.5.0 当前页只读预览 MVP

- 用户要求：在软件最右侧增加当前页展示功能，用于边编辑边查看页面实际效果。
- 方案：新增第三栏只读预览；选择模块内页面后，把当前项目快照渲染为临时PPTX，再定位该实例在最终PPT中的实际页码，由 Microsoft PowerPoint/WPS 导出1600×900 PNG。
- 稳定性：Office 导图运行在独立子进程，默认35秒超时；后台线程、防抖和“只保留最新选择”避免切页卡住主界面；关闭软件时只结束本次预览子进程，不结束用户已有的 Office 进程。
- 界面联动：页面选择、页面属性、结构化字段和模块启用状态变化后自动刷新；选择模块而非页面时显示操作提示；禁用模块明确提示其不属于最终PPT。
- 核心文件：`ppt_generator/preview.py`、`ppt_generator/office_preview.py`、`ppt_generator/ui/slide_preview.py`；规格为 `SPEC_CURRENT_PAGE_PREVIEW_MVP.md`。
- 自动验证：新增物理页码、禁用模块、可注入导出器和UI选择联动测试；完整测试与编译检查通过。
- 实际验证：Microsoft PowerPoint 成功导出 `output/current_page_preview_v050.png`；Windows界面截图 `output/ui_current_page_preview_v050_windows.png` 显示最右侧预览、页面名称、实际页码和导出后端。
- 剩余边界：本机未安装WPS，WPS导图适配尚未实机验证；预览为只读画面，不支持在画面内拖动对象、动画或视频播放。

### 2026-08-23｜实现 0.5.1 当前页预览性能优化

- 用户反馈：当前页展示等待时间偏长；允许页面图片在后台慢加载，但界面切换需要更及时。
- 性能基线：优化前同一真实页面连续两次均重新生成23页PPT并重启PowerPoint，耗时8.728秒、8.660秒。
- 单页渲染：新增 `render_project_page()`，只复制和替换目标页面，同时继续使用完整项目 `SlideContext` 的实际页码、模块编号和字段值；临时PPTX由23页减为1页。
- Office复用：新增持久化 `office_preview_server.py`，软件运行期间复用独立PowerPoint/WPS实例；每次只打开一页临时PPT，软件关闭或超时时结束该私有进程，不影响用户已有Office窗口。
- 延迟显示：加载新页面时不再清空右侧画面，保留上一次成功图片并显示加载说明；新PNG完成后一次替换。
- 缓存：按模板、目标页面、完整结构编号、解析后的字段值和图片文件时间计算指纹，最多保留32张内存预览；手动“刷新”绕过缓存。
- 实测结果：Microsoft PowerPoint冷启动3.365秒，后续两次1.105秒、1.167秒；Windows界面实际切换未缓存页面1.809秒，返回已缓存页面0.023秒。
- 验证：单页PPTX仅1个Slide XML、可由`python-pptx`重开、原模板哈希不变；持久进程复用、缓存指纹、UI缓存命中和完整回归测试通过。
- 产物：`output/ui_current_page_preview_v051_windows.png`；状态栏显示“内存缓存”，中文和页面画面正常。
- 剩余边界：第一次预览仍需等待Office冷启动约3秒；WPS因本机未安装尚未测量；缓存仅在本次软件运行期间保留。

### 2026-08-23｜实现 0.5.2 两级预览与相邻页预加载

- 用户决定：按建议增加明确加载状态、模板图到实时图的两级预览，以及前后相邻页预加载。
- 界面：切页后在画面中央显示半透明加载遮罩，明确区分“模板页面”、“上一次画面”和最终实时内容，避免误认旧图已是新页。
- 两级预览：模板来源页首次访问时单页导出，按模板 SHA-256 保存到 `output/.preview_cache/`；后续访问立即显示，再由实时页替换。
- 稳定性调整：放弃启动时批量导出23页，因本机PowerPoint整套导出会异常且可能长时间卡住；改为当前来源页按需导出，不增加启动负担。
- 中文路径兼容：NAT6704原始中文文件名直接调用PowerPoint `Slide.Export` 会返回 `0x80070002`；导出时仅复制为临时英文文件名，完成后自动清理，原模板不变。
- 预加载：当前实时页完成后，在没有新用户请求时依次生成下一页和上一页；新选择始终优先，不增加Office实例。
- 实测：Windows原生界面切到未缓存页约1.078秒，返回已缓存页约0.036秒；最终内存缓存2页。
- 验证：29项自动化测试与完整Python编译检查通过；原模板SHA-256仍为 `795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`。
- 产物：`output/ui_two_level_loading_v052_windows.png`、`output/ui_two_level_preview_v052_windows.png`；加载遮罩、模板图与最终图已人工查看正常。
- 剩余边界：WPS因本机未安装仍未实机验证；模板来源页在本机首次访问时仍需一次Office导图。

### 2026-08-23｜实现 0.6.0 光学 FAR 一键生成检测效果

- 用户输入：统一格式光学 FAR `templates/光学资料/NAT6801FAR(8.5).xlsx`；复用 DCE 已验证的表头、合并单元格和图片锚点识别思路，在 PPT 项目内实现独立解析器。
- 解析结果：项目编号 NAT6801、生产节拍 `40-50pcs/min`、5条检测项、3个工位、21张检测图；设备聚合为 `1200万×1；500万×2`、`FA镜头×2；远心镜头×1`、`同轴光×1；背光×3`。
- 项目映射：原5张检测效果页替换为21张第11页单图模板实例；同步更新第16页9×5产品检测项表和第10页7×4设备参数表，其余参数保留模板原值。
- 界面：Excel 工作台增加独立绿色“一键生成检测效果”按钮；应用后自动刷新模块树、字段、来源、图片素材、预计页数和当前页预览。
- 可重复性：检测图按 FAR SHA-256 保存至 `output/far_assets/`；重复应用同一 FAR 会替换旧检测页并复用21条素材记录，不重复叠加。
- PowerPoint兼容修复：同一来源页批量复制时移除无视觉作用、只能由单页持有的 WPS/PowerPoint Tags 元数据，避免 ZIP 可重开但桌面 PowerPoint 拒绝打开；增加对应回归测试。
- 真实输出：`output/NAT6801_FAR_OneClick_v0.6.0_Final.pptx` 共39页、约48MB；对应项目文件为 `output/NAT6801_FAR_OneClick_v0.6.0_Final.pptproj.json`。
- 验证：34项自动化测试和完整Python编译检查通过；真实界面调用后显示39页和21条图片素材；ZIP成员无损坏、`python-pptx`可重开39页、21张检测图和目标表格内容正确、所有克隆页无单页Tag关系；Microsoft PowerPoint 已实际打开最终成品且无修复提示。
- 原模板：SHA-256仍为 `795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`，未修改模板文件。
- 界面产物：`output/ui_optical_far_one_click_v060_windows.png`，Windows字体、按钮位置和布局已检查。
- 剩余边界：本机未安装WPS，仍需在WPS环境验证打开、版式和另存；检测标准与检测可行性必须由人工审核，软件不自动承诺检出率。

### 2026-08-23｜修订 0.6.1 检测效果页生成规则

- 用户纠偏：检测效果不按每张图片生成；以“检测工位＋检测项”为唯一单位，每项只生成1页，不同工位的同名检测项分别保留。
- 图片规则：当前工位有对应检测项图时取第一张；没有时使用当前工位第一张OK图；不跨工位借图，也不把原始图片数量当成页数。
- NAT6801结果：工位1（俯视）4页，均回退工位1 OK图；工位2（仰视）4页，分别使用指纹、镀层不良、生锈、磨伤的第一张匹配图；工位3（侧视）1页，使用第一张翘曲图；共9张检测效果页。
- 页面标注：标题和说明明确显示“工位1：俯视”“工位2：仰视”“工位3：侧视”，视角来自 FAR 的“工位视角”。
- 节拍规则：设备参数“生产节拍”只读取 Excel 的“检测速度”，NAT6801结果为 `40-50pcs/min`。
- 重复导入：按工位和检测项重建页面，测试数据即使同一工位重复出现“指纹”仍只生成1页；重复应用后仍为9页、6条实际选用图片素材。
- 真实输出：`output/NAT6801_FAR_OneClick_v0.6.1_Final.pptx` 共27页、约36.6MB；项目文件为 `output/NAT6801_FAR_OneClick_v0.6.1_Final.pptproj.json`。
- 验证：34项自动化测试和完整Python编译检查通过；ZIP无损坏，`python-pptx`重开为27页，9页工位/检测项/图片路径均与规则一致，设备节拍和9×5检测项表正确；Microsoft PowerPoint 已实际打开新版成品且无修复提示。
- 原模板：SHA-256保持 `795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`，未修改模板文件。
- 历史文件：0.6.0的39页文件属于旧的“每图一页”规则，不再作为当前结果；为避免覆盖既有产物，新版使用0.6.1文件名。
- 剩余边界：同一检测项有多张图时当前取第一张，后续仍可在“方案模块”人工替换；WPS因本机未安装待实机验收。

### 2026-08-23｜修订 0.6.2 无图检测项合并OK页

- 用户再次纠偏：同一工位只有OK图时，不按检测项重复生成多页；全部无图检测项写在同一页，只展示1张OK图。有对应缺陷图时仍保持每个缺陷1页、每页1张图。
- 通用规则：同一工位有图项各自生成缺陷页；没有对应图的检测项合并成1张OK页；混合工位最多增加1张OK合并页，不跨工位借图。
- NAT6801结果：工位1（俯视）由4张重复OK页合并为1页，页面分两行写明指纹、镀层不良、生锈、磨伤并只用 `station_01_image_001.png`；工位2（仰视）4项各1页；工位3（侧视）翘曲1页。
- 页面结构：检测效果共6页，分别为工位1 OK合并页、工位2四张缺陷页、工位3翘曲页；每页保留“工位N：视角”、相机、镜头、光源和检测范围。
- 完整输出：`output/NAT6801_FAR_OneClick_v0.6.2_Final.pptx` 共24页、约36.6MB；项目文件为 `output/NAT6801_FAR_OneClick_v0.6.2_Final.pptproj.json`。
- 数据结果：实际引用6张唯一图片；生产节拍继续只读取Excel“检测速度”，为 `40-50pcs/min`；产品检测项表仍为9×5。
- 验证：34项自动化测试与完整Python编译检查通过；重复工位检测项仍被去重，重复应用保持6页和6条素材；ZIP无损坏、`python-pptx`重开24页、原模板哈希不变；Microsoft PowerPoint 已实际打开新版成品且无修复提示。
- 历史文件：0.6.1的27页文件属于过渡规则，不再作为当前结果；新版单独使用0.6.2文件名，未覆盖旧产物。
- 剩余边界：同一缺陷有多张图时仍取第一张；WPS因本机未安装待实机验收。

### 2026-08-23｜修订 0.6.3 OK样件图语义

- 用户澄清：工位1只有1张OK图，表示没有其它缺陷样图；括号只需列明该页关联检查哪些缺陷，不能让文字看起来像OK图展示了这些缺陷。
- 页面文字：工位1主文字调整为两行 `OK样件图（指纹、镀层不良、生锈、磨伤）`；底部明确写明“本工位没有其它对应缺陷样图，本页展示1张OK样件图”。
- 保持不变：工位1仍为1页1张OK图；工位2四个缺陷各1页；工位3翘曲1页；检测效果6页、完整PPT24页、实际引用6张图片。
- 真实输出：`output/NAT6801_FAR_OneClick_v0.6.3_Final.pptx`；项目文件为 `output/NAT6801_FAR_OneClick_v0.6.3_Final.pptproj.json`。
- 验证：34项自动化测试与完整Python编译检查通过；ZIP无损坏、`python-pptx`重开24页、工位1正文/图注/说明语义正确、原模板哈希不变；Microsoft PowerPoint 已实际打开且无修复提示。
- 历史文件：0.6.2页数和图片逻辑正确，但OK页文字语义不够明确；当前以0.6.3为准。

### 2026-08-23｜实现 0.7.0 设备流程与设备功能模块 MVP

- 用户确认：工程师或AI先完成设备方案及方案图，软件负责结构化管理流程、实体设备模块和PPT页面；第一版不做Visio式自由画布。
- 项目模型：项目文件升级为schema v3；新增整机方案、流程节点和设备功能模块，流程按稳定ID关联实体模块；旧schema v1/v2仍可读取并在保存时升级。
- 界面：“方案模块”增加“PPT模块/设备方案”二级工作区；设备方案包含整机方案、流程设计和设备功能模块，支持增删排序、工位、动作、节拍、输出、图片、版式和关联编辑。
- 流程生成：现有模板第4页经视觉核对后确定为8个主流程位置，其他位置属于视觉辅助和OK/NG分支；超过8步自动复制流程页，未使用Slot清空。
- 设备生成：第5页同步整机图和说明；第6～9页登记为设备模块页面蓝图，一实体模块生成一页；缺少模块方案图时阻止同步，避免模板样图进入正式方案。
- 模板清理：页面蓝图声明需删除的旧机械标注Shape ID；Renderer复制页面后先清除旧标注，再写入新标题、说明、图片、图题和备注，避免“新图＋旧标注”。
- 关联保护：删除仍被流程引用的实体设备模块时默认拒绝；界面确认后可删除并同时清空关联。
- 验证产物：`output/设备方案流程模块_v0.7.0_验收版.pptx`和对应项目文件；包含8步流程、4个关联设备模块和23页完整技术方案。
- 自动验证：37项测试与完整Python编译检查通过；PPTX ZIP无损坏、23个Slide XML、schema v3项目含8个流程节点和4个实体设备模块、原模板SHA-256保持不变。
- PowerPoint验收：Microsoft PowerPoint实际导出流程页和设备模块页PNG；流程箭头、OK/NG/待复检分支正常，设备模块页旧标注已清除且文字图片布局正常。
- 剩余边界：当前未接入AI Provider，不自动判断机械方案可行性；本机未安装WPS，仍需WPS打开、版式和另存验收；正式内容等待用户真实设备图和工程说明。

### 2026-08-23｜制作工业单主线流程图样本

- 范围：依据工业流程图调研结论，为当前NAT6704技术方案模板制作一页可编辑样本；本轮只验证版式，不改正式流程Renderer和软件默认模板。
- 设计：保留模板顶部导航和企业标识，正文改为7步水平单主线卡片；检测工位使用工业蓝轻强调，普通工序保持中性，OK/NG状态只在最终分类下料节点出现。
- 内容：产品输入→输送定位→工位1俯视检测→产品翻转→工位2仰视检测→结果判定→分类下料；每个节点包含派生步骤号、类型、标题和单行说明。
- 产物：`output/设备单主线流程图_样本_v1.pptx`；可通过Microsoft PowerPoint继续编辑全部卡片、文字和箭头。
- 可复现脚本：新增`tools/build_linear_flow_sample.py`，默认保护已有输出，只有显式`--overwrite`才覆盖同名样本。
- 验证：脚本检查Office ZIP签名、必要包成员、1页页数和关键文字；`python-pptx`可重开；新增脚本编译通过；项目37项自动化测试全部通过。
- 实机检查：Microsoft PowerPoint已成功打开样本且无文件修复提示；本轮后台COM导图受本机已有PowerPoint进程状态影响未成功，前台查看和PPTX结构不受影响。
- 下一步：用户审核样本的节点大小、颜色和信息密度；确认后再把该样式接入设备流程页面生成器和软件默认模板。

### 2026-08-24｜制定设备介绍PPT模块方案

- 输入：`templates/设备模块提示词.txt`，并复核现有NAT6704第5～9页、设备方案schema v3、页面蓝图和当前模块生成逻辑。
- 关键判断：设备总览与模块分区在模块少或图片重复时应合并；四种版式定义为四种页面职责，而不是四套不一致风格；CAD/三维图默认完整适配，不裁切关键机构。
- 方案：建立设备总览、模块分区、单模块介绍、模块细节四类页面；单模块介绍按启用模块自动重复，细节页只在至少有2张有效细节图时按需生成。
- 内容边界：检测流程讲产品怎么走，设备介绍讲设备由什么组成，检测效果/视觉系统证明检测能力；完整参数表仍保留在设备参数章节。
- 数据设计：提出设备级核心指标和模块级功能、配置、动作步骤、细节图、标注位置等结构化字段；正式实现建议schema v4并兼容迁移v3。
- 文件：新增`SPEC_EQUIPMENT_INTRO_PPT.md`，并加入`README-AI.md`读取顺序。
- 验证：本轮为方案和文档更新，未修改运行代码、模板或PPTX，因此未运行自动化测试；已核对Spec章节、文件链接和项目边界。
- 下一步：用户审核页面数量、总览/分区合并规则和细节页条件；确认后先制作A～D四张真实样页，再进入数据模型与软件实现。

### 2026-08-24｜制作设备介绍A～D四页真实样本

- 范围：按`SPEC_EQUIPMENT_INTRO_PPT.md`第一实施阶段制作设备总览、模块分区、单模块介绍和模块细节四张可编辑PPT样页；不修改正式模板、schema、桌面界面或Renderer。
- 图片：直接从原NAT6704模板关系中读取整机等轴测图、俯视布局图、视觉移载结构图、翻转执行位和输出衔接位；CAD/三维图全部采用完整适配，不做破坏性裁切。
- 页面：A页展示整机图、定位说明和6个样本指标；B页展示4个模块编号及功能图例；C页展示模块定位、3项功能、4项配置和5步动作链；D页展示1张整体图和2个局部细节。
- 产物：`output/设备介绍_A-D四页样本_v1.pptx`，共4页、6张图片，文字、参数卡片、编号、标注和流程步骤均为PowerPoint可编辑对象。
- 可复现脚本：新增`tools/build_equipment_intro_samples.py`，默认保护已有输出，只有显式`--overwrite`才覆盖同名样本；生成时检查原模板哈希不变。
- Office结构：PPTX ZIP测试无损坏、必要包成员存在、`python-pptx`可重开4页；原模板的版式/备注关系会保留一个内部Slide部件，因此验收以演示文稿可见页关系为准，不破坏模板兼容关系。
- 版面验证：所有Shape均位于幻灯片边界内；Microsoft PowerPoint前台打开、缩略图和放映显示正常，无文件修复提示。
- 自动验证：新增脚本编译通过，项目37项自动化测试全部通过。
- 当前限制：本机PowerPoint后台COM导图在当前Windows登录会话中挂起，已终止调用并改用前台检查；Windows高DPI导致系统截图只能分区查看；WPS仍待安装环境验收。
- 下一步：用户审核四页样本的图文比例、配色、指标密度和模块细节形式；确认后再进入schema v4、结构化编辑界面和正式自动生成实现。

### 2026-08-24｜设备介绍工业版式调研与检测工位详页二次方案

- 用户反馈：A～D首版样本的圆角图片、胶囊标签和浅色卡片缺少工业感；要求中文/英文/数字字体统一、每张图增加规范图题，并补充逐工位检测模块详页，同时检查与检测效果/视觉系统页面的冲突。
- 官方参考：筛选 Beckhoff XPlanar、OMRON FH视觉系统/制药检测/PCB检测、SICK Nova Intelligent Inspection、KEYENCE机器视觉资料；参考文件仅嵌入带来源的缩略截图用于内部版式审核，不照搬厂商品牌视觉。
- 视觉结论：后续样页取消圆角图片蒙版、胶囊标签、重阴影和大面积卡片；改为完整工程主图、直角边界、细分隔线、线性引线和传统图下说明。中文、英文和数字统一声明 `Microsoft YaHei`。
- 图题规则：每张正式图片下方增加可编辑图号/图题，例如 `图 3.1 设备总体图`；图号由章节结构派生，模块和工位排序变化后统一重算。
- 页面边界：检测模块不少于2个工位时生成1页总览；每个启用工位生成1页详页。工位详页负责工位结构、产品姿态、检测项、相机/镜头/光源、触发、节拍和输入输出；它替代通用“视觉检测模块介绍页”。检测效果页继续只放OK/NG证据，视觉系统总页只放共用平台/架构，避免重复。
- 文件：新增 `tools/build_industrial_reference_pack.py`，生成 `output/设备介绍_工业版式参考与工位页方案_v1.pptx`；同步修订 `SPEC_EQUIPMENT_INTRO_PPT.md`。
- 自动验证：参考文件共9页、9个Slide XML、9个媒体对象，PPTX ZIP无损坏且可由`python-pptx`重开；所有可编辑文字字体均为`Microsoft YaHei`，无圆角矩形、无越界Shape，包含9条官方参考图题和2条目标样式图题。
- 实机边界：当前PowerPoint COM预览受本机已有Office进程状态影响，未完成自动导图；未终止或干扰用户现有PowerPoint进程。参考文件本身已通过结构检查，WPS仍待实机验收。
- 下一步：用户确认推荐的“Beckhoff式工程主图 + OMRON式单工位结构”后，重做A～D四页并新增1张工位详页样本；确认以前不修改正式Renderer、schema和桌面界面。

### 2026-08-24｜重做设备介绍工业版五页样本与紧凑导航栏

- 用户确认工业版式方向，并提出统一背景页顶部的“企业简介、工艺分析、设备布局、模块介绍、供货范围”应改成导航栏，原高度偏高且需要可调。
- 导航设计：保留5个章节，取消整块红色激活背景，改用红色文字和底部细线标识当前章节；原模板导航高约`0.892 in`，新版默认`0.52 in`，约减少42%，允许通过脚本参数在`0.42～0.72 in`范围调整。
- 联动规则：导航背景、章节文字、分隔线、当前项下划线和Logo按同一高度计算；标题及正文起点随导航高度联动，避免单独缩放造成重叠。当前样本页1～2高亮“设备布局”，页3～5高亮“模块介绍”。
- 页面重做：生成设备总体方案、功能模块分区、搬运与翻转模块、模块结构细节、工位1俯视检测共5页；检测工位详页取代通用视觉模块介绍页，不与检测效果页重复职责。
- 视觉规则：取消圆角图片与胶囊卡片，使用直角工程图、细线分区、线性标注和图下说明；中文、英文、数字统一声明`Microsoft YaHei`，8张图片均有可编辑图号/图题。
- 真实数据：工位1详页读取`NAT6801FAR(8.5).xlsx`，显示俯视工位、4项检测内容、500万相机、FA镜头、同轴光/背光和`40-50pcs/min`检测速度。
- 产物：`output/设备介绍_工业版五页样本_v2.pptx`；可复现脚本为`tools/build_equipment_intro_samples.py`，支持`--nav-height`和显式`--overwrite`。
- 验证：默认高度及`0.42/0.72 in`边界均生成成功，`0.30 in`被明确拒绝且不产生文件；PPTX共5页、约13.2MB，Office ZIP、页数、图片/图题、字体、无圆角、边界和模板哈希检查通过；项目37项单元测试及完整Python编译检查通过。
- 实机边界：当前Windows会话的PowerPoint后台COM导图仍受既有Office进程状态影响，未强制终止用户进程；本机未安装WPS。结构检查已通过，仍需用户分别用PowerPoint/WPS做最终视觉与另存验收。
- 后续：用户确认导航默认高度、图文密度与工位页字段后，再将本轮样式配置接入正式Renderer、schema v4和桌面界面；当前没有覆盖原模板。

### 2026-08-24｜实现 0.7.1 导航样式闭环并纠正检测模块页

- 用户反馈：导航文字偏小且不够醒目，要求加粗并放大一级；软件需要可选择导航背景色；检测模块页不能与检测效果混淆，只展示对应检测模块图和所含工位名称。
- 导航文字：五个章节统一由`8.8 pt`提升为`10 pt`并全部加粗；当前章节继续使用底部细线标识。浅色背景使用红色当前项和深灰其它项，深色背景自动切换为高对比文字、分隔线与下划线。
- 软件界面：在右侧“模板与输出”下方新增“导航样式”，可调`0.42～0.72 in`高度并通过颜色选择器设置`#RRGGBB`背景色；按钮同步显示当前色值和色块。
- 数据与生成：schema v3增加向后兼容的`presentation_style`字段，不提升schema版本；导航高度和背景色随项目保存/重开。正式`render_project`、单页`render_project_page`和预览指纹读取同一配置，修改样式后不会误用旧预览缓存。
- 检测模块纠偏：五页样本第5页由“工位1详页”改为“检测模块介绍”；使用模板内检测模块工程图，只列出`俯视检测工位、仰视检测工位、侧视检测工位`，不使用FAR中的OK/NG图，也不显示检测项、相机、镜头、光源、触发、节拍或判定结果。
- 样本脚本：`tools/build_equipment_intro_samples.py`新增`--nav-background`；默认成品继续为`output/设备介绍_工业版五页样本_v2.pptx`，共5页、7张工程图/图题，原模板不被覆盖。
- 验证：深色`#123456`背景生成成功，非法色值被拒绝且不产生文件；正式Renderer使用真实NAT6704模板验证`0.60 in + #DDEEFF`并保持原模板哈希；PPTX ZIP、页数、导航字号/加粗、背景色、无圆角、无越界、字体和检测模块禁用字段检查通过。
- 自动化：项目40项单元测试及完整Python编译检查通过；新增项目样式往返、非法色值、UI控件、预览缓存和真实模板Renderer测试。
- 实机边界：本机仍未安装WPS；PowerPoint当前Windows会话的后台COM导图问题未强制处理。新版PPTX可前台打开供用户视觉审核。
- 下一步：用户确认导航默认高度/颜色和检测模块页后，再把检测模块图、工位名称列表接入设备介绍正式页面蓝图与结构化编辑区。

### 2026-08-24｜实现 0.7.2 自动方案独立审核模块

- 输入：`templates/自动方案模块提示词.txt`；目标流程为客户需求结构化、历史/标准模块检索、候选方案、工程审核、局部纠正、正式方案图和固定PPT输出。
- 架构判断：现有Excel/Word/FAR解析、设备流程/模块、PPT模板Renderer和预览可以后续复用；当前缺少Requirement JSON、来源/置信度、历史/模块库、确定性工程规则、局部版本和人工锁定，不能把AI输出直接写入现有PPT字段。
- 隔离策略：主窗口新增第5个顶层页签“自动方案（试验）”；本轮不修改`PptProject` schema、不随`.kyppt.json`保存、不调用AI/数据库、不连接Renderer，也不改变正式项目模块和页数。
- 界面：左侧六阶段可点击导航，分别为客户需求、历史/模块检索、候选技术方案、工程规则审核、局部纠正和输出准备；右侧显示各阶段字段、候选表、规则范围、问题列表、版本/锁定与输出检查。
- 需求页：固定11类需求字段，每项显示当前值、`unknown/need_confirm/confirmed`状态、来源类型和来源说明；空值不自动补写。
- 方案页：支持在独立草稿中增加/删除候选工位、编辑功能/模块/节拍、显示来源、0～100%置信度及A/B/C等级，并可人工锁定工位；不写入现有设备方案。
- 安全边界：检索、工程审核、局部纠正按钮在引擎实现前保持禁用；“合并到正式项目/PPT”明确禁用。AI推测不能标记为已确认，存在未解决阻断问题时ValidationResult不能通过。
- 核心模型：新增UI无关的`RequirementModel`、`SolutionModel`、`ModuleModel`和`ValidationResult`，并包含EvidenceRef、工位来源/置信度、模块验证状态和问题严重度/阻断状态。
- 规格：新增`SPEC_AUTO_SOLUTION_MODULE_MVP.md`，记录现状差距、可复用能力、目录边界、四类模型、模块关系、六阶段界面和后续分阶段计划。
- 验证：新增4项核心模型测试和1项界面隔离/导航测试；项目完整45项单元测试及完整Python编译检查通过。新增界面不改变原有16个PPT模块，现有Excel、FAR、设备方案、Renderer和预览测试全部保持通过。
- 下一步：用户审核六阶段名称、Requirement字段、候选方案表格和输出边界；确认后优先实现独立草稿保存及Excel/Word/PDF到Requirement JSON映射，再进入历史/标准模块库。

### 2026-08-24｜实现 0.7.3 横向预览工作区并修复检测流程同步后空白

- 问题根因：设备方案同步会重新生成检测流程/设备模块页面并分配新的页面 UUID；模块树仍按旧 UUID 恢复选择，找不到后退回模块节点，主窗口收到“只有模块、没有页面”的选择后会按设计清空当前页展示。
- 选择恢复：`ModuleEditor`新增稳定的“模块ID + 页序号”锚点；方案同步后优先恢复同一模块的同一页序，新页数变少时落到最后一页，模块无页时才保留模块节点。
- 主布局：原“左侧项目 + 中间模板输出 + 最右窄预览”三列改为两列；左侧保留当前项目和五个功能页签，右侧改为纵向审阅区。
- 右侧上方：当前页展示优先占用空间，画布最小尺寸由`340 × 220`提升为`480 × 270 px`，图片继续保持源页面比例缩放。
- 右侧下方：新增“模板与输出 / 结构与记录”两个自适应页签；模板路径、输出路径、导航样式和生成检查采用横向网格，PPT结构与运行记录采用可拖动水平分隔。
- 尺寸检查：在默认`1800 × 940`窗口中左右区域约`860 / 941 px`，右侧上下约`533 / 304 px`；在最小`1280 × 720`窗口中画布仍为`530 × 270 px`，没有低于横向预览下限。
- 规格：`SPEC_CURRENT_PAGE_PREVIEW_MVP.md`更新为0.5.3，记录新布局、选中页恢复行为与验收标准。
- 验证：新增右侧布局和检测流程页面 UUID 重建后的回归测试；项目完整47项单元测试及完整Python编译检查通过。
- 下一步：用户在实际桌面界面审核预览高度、左右宽度和下方工具密度；若需要，可继续保存分隔条位置或增加“收起下方工具区”。

### 2026-08-24｜左上角软件名称显示统一版本号

- 界面：左上角名称调整为`AI PPT Studio v0.7.3`。
- 版本来源：界面直接读取包级`__version__`，后续升级版本号时无需再次修改标题文字。
- 验证：增加左上角名称与统一版本变量一致的界面断言；项目完整47项单元测试和规定的Python编译检查全部通过。

### 2026-08-24｜实现 0.7.4 项目级顶部导航栏目编辑

- 默认栏目：顶部导航重新划分为`公司简介、工艺分析、设备设计、检测效果、系统介绍`，旧项目未保存栏目配置时自动使用新默认，不提升schema版本。
- 默认归属：公司简介包含封面/公司介绍/公司资质/版本记录/结束页；工艺分析包含需求分析/检测流程；设备设计包含设备总览/设备模块/设备参数；检测效果包含检测效果/检测项目；系统介绍包含视觉系统/工控系统/AI算法/主要部件。
- 数据模型：新增UI无关的`NavigationItem`，每项保存名称和模板模块key；`PresentationStyle`支持1～7项、名称不超过10字符、名称唯一和模块唯一归属，并随项目JSON往返保存。
- 软件界面：“导航样式”增加当前栏目摘要和“编辑栏目”；弹窗支持新增、重命名、分配模块、删除、上移和下移，取消不写回项目，同一模块重新分配时自动从原栏目移除。
- PPT生成：兼容模板布局中的旧固定文字和分隔线被隐藏；最终生成副本在每页使用可编辑Shape绘制项目导航，并按`SlideContext.template_module_key`设置活动文字和下划线。预览与最终PPT使用同一配置，配置变化会更新预览指纹。
- 自适应：导航按可用宽度自动等分；真实模板默认5项和新增第6项均通过，字体保持Microsoft YaHei 10 pt加粗，深浅背景对比规则不变。
- 样本兼容：`tools/build_equipment_intro_samples.py`改为页面建立后再应用新导航，临时5页工业样本生成和内置结构检查通过。
- 规格：新增`SPEC_NAVIGATION_EDITOR_MVP.md`并加入AI读取顺序。
- 结构验证：真实NAT6704完整生成23页、23个Slide XML，ZIP签名和必要结构有效，`python-pptx`可重开；第2/3/5/11/17/21页分别验证公司/工艺/设备/检测/系统/公司高亮，源模板SHA-256不变。
- 自动化：新增旧项目默认补齐、项目往返、非法配置、编辑弹窗、模块唯一归属和第六栏渲染测试；项目完整50项单元测试及规定的Python编译检查通过。
- 剩余验证：当前Windows登录会话的PowerPoint COM返回“服务器运行失败”，自动PNG导图未完成；未结束用户Office进程。本机未安装WPS，仍需用户在PowerPoint/WPS中检查实际视觉和另存。

### 2026-08-24｜修正 0.7.5 导航背景范围、等宽红线和即时预览

- 交互修正：软件中的“导航背景”改名为“当前栏目背景”，避免误解为整条导航栏颜色；选色仅影响当前页面所属栏目，其它栏目保持白色。
- PPT修正：最终页面新增当前栏目独立背景Shape；背景和底部红线均使用当前栏目文字格的同一左右边界与宽度，删除原有左右缩进。
- 即时预览：当前页画布保留一份未叠加的基础图；调整高度、颜色或栏目配置后立即重绘导航近似效果，不等待PowerPoint/WPS导图，后台真实预览完成后自动替换。
- 回归保护：增加像素级界面测试，检查当前栏目着色、非活动栏目白色、红线颜色及高度缩小时旧画面不残留；真实NAT6704渲染测试检查当前栏目背景和红线与文字格等宽。
- 规格与版本：更新`SPEC_NAVIGATION_EDITOR_MVP.md`、README和AI接手说明；统一软件标题、包版本及构建元数据为`0.7.5`。
- 验证：完整51项单元测试和规定的Python编译检查通过；生成`output/导航栏当前栏目范围样本_v0.7.5.pptx`，Office ZIP签名、必要成员、成员CRC、5页重开和导航Shape结构检查均通过。
- 下一步：用户在实际界面确认导航高度变化速度、颜色范围和红线视觉；如需继续细调，可再开放红线高度、字体大小或各栏目宽度配置。

### 2026-08-24｜实现 0.7.6 导航自适应字号与统一底部条带

- 字号模式：项目样式新增向后兼容的`navigation_font_size`；缺失或为空时使用自动模式，`0.52 in`对应`10 pt`，随导航高度按比例增大，范围限制为`9～14 pt`。
- 手动设置：软件“导航样式”增加“导航字体”选择，可选自动或固定`9～16 pt`；手动字号优先并随项目保存/重开，修改后即时预览和最终PPT同时刷新。
- 条带格式：每个栏目都生成相同宽度、高度和垂直位置的底部条带；当前栏目填充红色，其余栏目填充白色，除颜色外Shape格式及几何尺寸一致。
- 预览同步：快速预览按解析后的实际pt字号绘制，并同时生成红色活动条带和白色非活动条带；改变高度或字号时从基础图重新绘制，不叠加旧画面。
- 兼容：旧项目缺少字号字段时自动使用新规则，不提升项目schema；原模板继续只读，PPT导航仍为可编辑页面Shape。
- 产物：`output/导航字号与等格式条带样本_v0.7.6.pptx`使用`0.72 in`自动得到`14 pt`；`output/ui_navigation_font_v076.png`记录新增字号选择界面。
- 验证：完整51项单元测试和规定的Python编译检查通过；样本通过Office ZIP签名、必要成员、CRC、5页重开、14 pt自动字号、5个同尺寸条带及红/白颜色检查；原模板哈希未变。
- 下一步：用户在PowerPoint/WPS中审核14 pt高导航样本；若自动比例偏大或偏小，只调整统一换算规则，不改变项目结构。

### 2026-08-24｜修正 0.7.7 导航阴影与高度即时预览可见性

- 根因复现：实际主窗口路径中，`0.52 → 0.72 in`会让红条从预览图约第58行移动到第82行，但即时画面随即被半透明加载遮罩覆盖；白色背景又缺少清楚的动态边界，导致肉眼判断为“没有变化”。
- 阴影来源：原NAT6704导航白色背景使用PowerPoint外阴影；新增红/白条带此前没有自己的阴影，因此红条覆盖白色边界后显得突兀。
- PPT修正：每个红/白状态条带均写入相同Office DrawingML外阴影：`4 pt`模糊、`1 pt`向下距离、黑色`18%`透明度；Shape的大小、位置、线条和阴影完全相同，只保留填充色差异。
- 即时预览：Qt快速画面为每个栏目绘制相同方向的渐隐阴影；高度或字号修改后隐藏加载遮罩，状态栏直接显示当前高度和实际字号，Office真实图仍在后台更新并最终替换。
- 回归：新增真实选中页面后的高度操作测试，比较修改前后PNG字节和红条垂直位置，并断言即时画面没有加载遮罩；导航Renderer测试逐条检查阴影XML参数。
- 产物：`output/导航阴影与高度即时预览样本_v0.7.7.pptx`采用白色当前栏目背景和`0.72 in / 14 pt`，用于突出审核红条与白条的统一阴影。
- 验证：完整52项单元测试和规定的Python编译检查通过；样本通过Office ZIP签名、必要成员、CRC、5页重开及每页5个同参数阴影条带检查；原模板SHA-256仍为`795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`。
- 下一步：用户只需判断阴影是否过强或过弱；后续如需微调，仅修改统一阴影参数，不再让红条和白条采用两套格式。

### 2026-08-24｜修正 0.7.8 连续导航阴影与栏目边界留白

- 用户纠偏：导航其它位置看到的是整条白色背景矩形的灰色外阴影，不是逐栏目白线；0.7.6/0.7.7把它错误拆成白色条带和条带阴影，导致红线与灰影视觉割裂。
- 模板证据：NAT6704兼容Layout中的`矩形 4`覆盖`0～13.333 in`，自身带`18 pt`模糊、`4 pt`向下距离、黑色`29%`透明度的连续阴影；原模板没有逐栏白色底线。
- 结构修正：删除所有`KY_NAV_INACTIVE_*`和红线独立阴影；保留Layout背景原始连续阴影，每个页面只在当前栏目生成一个`KY_NAV_ACTIVE`红线。
- 边界修正：导航栏目区域由`0.20～11.55 in`改为`0～11.55 in`无缝等分；公司简介从幻灯片最左侧开始，相邻栏目使用同一边界，最后一栏在Logo区前结束。
- 预览根因：模板缩略图保留原`0.892 in`导航的固定灰影，之前只清理到`0.80 in`，所以调整高度后会同时看到新旧两条边界。现在先移除旧导航和旧灰影，再按当前高度绘制一条连续灰影。
- 性能：清理后的基础图按当前页面缓存；第一次准备约`0.20 s`，之后连续高度/字号调整约`0.002 s/次`，不再重复逐像素清理。
- 产物：`output/导航连续阴影与无缝边界样本_v0.7.8.pptx`保留完整23页，20个导航页覆盖公司简介、工艺分析、设备设计、检测效果、系统介绍五种活动状态。
- 验证：完整52项单元测试和规定的Python编译检查通过；完整样本为23页、20个导航页、5种活动栏目，所有栏目从`0`连续排列到`11.55 in`且无`KY_NAV_INACTIVE_*`；Office ZIP、CRC和重开检查通过，设备五页脚本样本也通过；原模板哈希未变。
- 下一步：用户重点审核五种活动状态的红线端点；如仍有视觉偏差，只调整统一栏目终点`11.55 in`或红线厚度，不再引入白条。

### 2026-08-24｜修正 0.7.9 导航灰色阴影突兀问题

- 用户结论：连续灰色阴影仍与当前栏目红线产生突兀感，明确要求完全去掉灰色阴影。
- 最终PPT：生成副本时删除兼容Layout导航背景中的`a:outerShdw`，保留白色背景、栏目分隔线和当前栏目唯一的`KY_NAV_ACTIVE`红线；源模板不修改。
- 即时预览：继续清理模板缩略图中原始固定灰影，但不再按当前高度绘制Qt渐变替代阴影；红线下方直接显示白色页面背景。
- 回归标准：布局背景无外阴影、页面无`KY_NAV_INACTIVE_*`、栏目连续覆盖`0～11.55 in`、当前红线与栏目等宽，预览红线下方像素为白色。
- 产物：`output/导航无灰色阴影样本_v0.7.9.pptx`，保留完整23页，其中20个导航页只含当前栏目红线。
- 验证：完整52项单元测试和规定的Python编译检查通过；样本通过ZIP签名、必要成员、CRC、23页重开检查，5个兼容Layout的导航背景外阴影数量为0，20个导航页无`KY_NAV_INACTIVE_*`且各有1条`KY_NAV_ACTIVE`；原模板SHA-256仍为`795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`。
- 下一步：用户在软件即时预览及PowerPoint/WPS中审核无灰影导航；若视觉仍需调整，只讨论红线厚度、分隔线或栏目高度。

### 2026-08-24｜修正 0.7.10 区分导航底线与阴影

- 用户纠偏：要求去除的是模糊灰色阴影效果，不是导航底部线条；0.7.9只保留活动红线，导致其它栏目下方完全无线条。
- 设计修正：导航底部增加一条覆盖`0～11.55 in`的连续实色灰线`KY_NAV_BASELINE`，当前栏目在同一纵向位置覆盖等高、等宽红线`KY_NAV_ACTIVE`，避免逐栏拼接留白。
- 阴影边界：Layout背景、灰色底线和红线均不含Office`a:outerShdw`；即时预览只绘制实色灰/红线，不使用Qt渐变阴影。
- 回归标准：非活动栏目底线为`#D3D9DE`、活动栏目为`#C90000`，两者高度和纵向位置一致；线条下方仍为白色，栏目连续覆盖范围和字体规则不变。
- 产物：`output/导航灰色底线无阴影样本_v0.7.10.pptx`，保留完整23页及五种活动栏目状态。
- 验证：完整52项单元测试和规定的Python编译检查通过；样本通过ZIP签名、必要成员、CRC和23页重开检查。20个导航页各有1条灰色底线与1条活动红线，5个兼容Layout外阴影数量为0，活动状态覆盖全部5个栏目；原模板SHA-256仍为`795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`。
- 下一步：用户在软件即时预览及PowerPoint/WPS中审核灰线与红线的颜色、厚度和连续性。

### 2026-08-24｜修正 0.7.11 公司Logo区域底线连续性

- 用户补充：最右侧公司图标区域下方也需要保留同一条灰色实线，不能在最后一个导航栏目结束处中断。
- 结构修正：`KY_NAV_BASELINE`由栏目范围`0～11.55 in`延伸到16:9幻灯片全宽`0～13.333 in`；五个栏目及活动红线的范围仍保持`0～11.55 in`，不把Logo区误当作导航栏目。
- 预览同步：Qt即时预览的灰线延伸到图像最右边，并增加Logo区像素断言；底线和Layout仍无任何渐变或Office外阴影。
- 产物：`output/导航全宽灰色底线无阴影样本_v0.7.11.pptx`，保留完整23页和五种活动栏目状态。
- 验证：完整52项单元测试和规定的Python编译检查通过；样本通过ZIP签名、必要成员、CRC及23页重开检查。20个导航页的灰线均覆盖完整幻灯片宽度，活动红线均保持在`0～11.55 in`栏目区，5个兼容Layout外阴影数量为0；原模板SHA-256仍为`795ACCABC0BB6B6EFC619E585A17A2BCA8E03C8B418CE5F8223DF1962A058CDF`。
- 下一步：用户审核最右公司Logo区域下方灰线与栏目区灰/红线的连续性。

### 2026-08-24｜固化模块化开发与低耦合联动约束

- 用户要求：后续软件功能必须模块化开发；存在联动时要统一管理，方便局部修改并减少模块耦合。
- 模块边界：每个功能模块独立负责自身数据模型、业务服务、界面适配和针对性测试，模块内部修改不应要求其它模块同步改实现细节。
- 联动规则：跨模块流程统一由Application层协调，通过稳定ID、明确数据对象或事件传递；禁止PySide6控件互调、禁止跨模块直接读取内部对象。
- 状态规则：同一业务状态只设一个权威拥有者，消费者采用单向同步；不复制维护同一状态，不建立双向回调链。
- 复杂度边界：只有出现真实第二个复用方后才提取共享抽象，避免用过度分层代替实际模块边界。
- 文件：更新`AGENTS.md`、`PROJECT_FRAMEWORK.md`、`README-AI.md`和本记录；本次只固化工程约束，未修改运行时代码。
- 验证：核对四份文档均包含模块所有权、Application层联动、稳定ID/数据契约、单一状态来源和禁止UI互调规则；本次无运行时代码变更，因此未运行单元测试。
- 下一步：从下一项功能开始，Spec必须先写清模块所有者、输入输出、联动接口、状态权威来源和针对性测试，再进入实现。

### 2026-08-24｜实现 0.8.0 自动方案 v2 需求管理与候选方案

- 规格：依据`templates/自动方案-v2.txt`新增`SPEC_AUTO_SOLUTION_V2.md`，先定义模块所有者、数据契约、状态权威来源、解析确认边界和验收标准；原六阶段界面与正式PPT链路保留，不重做现有架构。
- 需求领域：新增正式`RequirementRecord`，保存原始需求、固定分类配置、九项产品状态、多项检测要求、状态、创建/更新时间和版本；原始需求不会被解析器修改。
- 管理功能：完成需求新建、查看/编辑、保存、复制、归档、受限删除和版本历史；JSON存储使用UTF-8原子替换，默认路径为`output/auto_solution_v2_store.json`，V1创建及后续V2/V3修改均保留前后快照、时间、操作人和动作。
- 解析边界：建立可替换`RequirementSuggestionProvider`，当前默认本地保守规则解析器；解析只生成字段路径、当前值、建议值、依据和置信度，界面勾选后也只写入空字段，不覆盖人工值。由于尚未配置在线Provider，本版本不宣称调用大模型。
- 历史检索：历史库只传递产品、尺寸、检测项、节拍、上下料、工艺、工位、模块、布局、关键参数和已知问题摘要；首版加权匹配会显示相似度和命中理由。内置三条记录全部标明`demo`，无匹配时保持空历史引用。
- 候选方案：新增`CandidateSolution`、结构化三工位、`DrawingSpecification`和Prompt Builder；Prompt明确产品位置、输入/输出方向、工位数量与相对位置、固定/移动部件、相机/光源/夹具以及禁止项。候选页支持重新生成、人工修改JSON/工位/流程、保存和确认；图像API与正式PPT合并继续禁用。
- 模块化：需求领域、存储、历史/候选生成、Application服务、需求界面、历史界面和候选界面分别落在独立文件；跨页面只传稳定需求ID，所有CRUD、检索和生成均经过`AutoSolutionApplication`，没有直接访问其它模块内部数据。
- 演示产物：`output/自动方案_v2_三组Mock结果.json`包含三组完整需求、建议、应用字段、历史匹配、候选方案、DrawingSpecification和Prompt；`output/ui_自动方案_v2_需求管理.png`与`output/ui_自动方案_v2_候选方案.png`展示最终页面。
- 版本：软件标题、包版本和构建元数据统一提升到`0.8.0`，主工作区名称改为“自动方案 v2”；README、AI接手说明、框架和质量门禁已同步。
- 验证：完整58项单元/界面测试通过；规定的完整Python编译检查通过；三组Mock结果再次解析确认`resultCount=3`、候选方案3份、Prompt全部非空、历史来源仅为`demo`，两张UI图片均成功生成。
- 下一步：用户审核分类字段、三工位默认结构、历史匹配权重和最终Prompt；确认后优先接入真实历史项目摘要导入与在线AI需求解析Provider，再进入工程规则审核，不提前合并正式PPT。

### 2026-08-24｜实现 0.8.1 独立方案图实验室

- 规格与边界：新增`SPEC_SCHEME_VISUAL_LAB_MVP.md`。本期只核验`DrawingSpecification → LayoutPlan → SVG → PromptRecipe`，不调用图片API、不读取或修改正式候选方案、不写PPT，也不把结构示意图宣称为CAD施工图。
- 确定性核心：新增`ppt_generator/scheme_visual_lab.py`，支持1～8个工位的单条水平主线布局；相同规范稳定产生相同规格哈希、布局哈希、SVG字节、PromptRecipe哈希、固定种子池和核验清单，不包含时间、UUID或随机坐标。
- 工程约束：工位数量、ID、名称和顺序严格来自输入；视觉部件和治具只有在工位字段明确填写后才会显示，禁止通过“检测工位”等名称自行补充结构；用户禁止项进入负向提示词和核验项。
- 独立界面：新增“方案图实验室（试验）”页签，可编辑JSON、校验并生成、缩放查看SVG、查看LayoutPlan/PromptRecipe/最终提示词/核验清单并导出SVG和Recipe。界面明确标注隔离边界，AI候选图按钮保持禁用，错误以内联状态显示。
- 模块边界：实验核心仅复用已有`DrawingSpecification`数据契约，不依赖`AutoSolutionApplication`、Repository、`PptProject`或Renderer；主软件唯一集成点是创建独立页签，没有界面互调或双向状态同步。
- 审核产物：`output/方案图实验室_结构示意图.svg`、`output/方案图实验室_PromptRecipe.json`和`output/ui_方案图实验室.png`。样本为ST01～ST03三工位，布局哈希`e24122b30dd4fce74ea3649d84c143a4f66f48567398a75373cc9daef36d1c2c`，Recipe哈希`bb309bb363471cae6fe07619121a8862b43ba449849c65904ea0c6ec3ee324b0`。
- 版本与文档：包版本、构建元数据和软件左上角版本统一提升到`0.8.1`；README、AI接手说明、项目框架和质量门禁同步登记。
- 验证：新增8项确定性、工位顺序、显式视觉部件、非法输入和离屏界面测试；完整66项单元/界面测试通过，规定的完整Python编译检查通过。SVG可由XML解析，工位标识严格为`ST01、ST02、ST03`，磁盘审核产物与服务重新生成结果完全一致。
- 下一步：用户先核验实验界面的字段、结构图样式、三工位表达和核验清单；确认后再决定是否增加正式DrawingSpecification导入、ComfyUI/ControlNet或商业图片Provider、同Recipe多种子候选和候选方案回写。

### 2026-08-24｜实现 0.8.2 无CAD设备逻辑方案编辑器

- 用户目标：没有CAD时先由软件和AI形成可用于售前PPT的设备方案；外观不要求接近真实设备，但上料、输送、定位、检测、分选和下料逻辑不能出错，复杂设备必须能够分模块调整。
- 规格：新增`SPEC_NO_CAD_LOGIC_SCHEME_MVP.md`，首版限定单条从左到右产品主线，优先验证模块选择、顺序、锁定、逻辑门禁和确定性中间产物；不提前接图片Provider、正式候选方案或PPT。
- 标准模块库：新增人工/振动盘/料盘/皮带上料、皮带/直线输送、阻挡/旋转定位、顶部/侧面/底部/多视角检测、气吹/推料分选、OK/NG/皮带下料共16种通用模块；每个模块含稳定类型ID、类别、用途、内部部件和结构图颜色。
- Scene核心：新增`EquipmentScene`、`SceneNode`和相邻`FlowLink`，支持添加、替换、删除、上移、下移、拖动排序、自动重建主线和确定性自动排布；同一Scene稳定产生相同哈希、SVG和AI生成约束。
- 锁定行为：已确认模块可锁定位置与结构；自动排布保留坐标，替换、删除、移动和改变排序均被服务层拒绝，必须先解除锁定。锁定导致坐标逆序或重叠时不会静默修正，而是进入逻辑门禁。
- 逻辑门禁：上料不在首位、下料不在末位、缺少检测、分选位于检测前、分选后再次检测、连接断裂、尺寸无效和坐标非左到右均为阻断项；缺少定位/分选、振动盘直接检测、工位编号问题和重叠为警告；阻断时`canGenerateAi=false`并切换红色状态。
- 独立界面：主入口更名为“无CAD方案实验室”，默认显示逻辑方案编辑器；左侧为模块库和可拖动产品主线，中间为SVG结构图，右侧编辑方案/模块属性并显示逻辑检查、Scene JSON和AI生成约束。原DrawingSpecification提示词/SVG实验作为第二页保留。
- 边界：本期结构图只表达售前逻辑，不判断强度、干涉、节拍、气缸选型和可制造性；AI按钮保持禁用。后续图片Provider只能读取通过门禁的Scene快照，不能直接访问或改写编辑器状态。
- 审核产物：`output/无CAD设备逻辑方案.svg`、`output/无CAD设备逻辑方案.scene.json`、`output/ui_无CAD设备逻辑方案_通过.png`和`output/ui_无CAD设备逻辑方案_阻断.png`。演示Scene为7模块主线，哈希`0c87c2ec9ed99b28b5b284f507c836574a632abcc7c1cb57917d4ec59caf1bbe`；阻断样本删除全部检测模块并显示`NO_INSPECTION`。
- 版本与文档：包版本、构建元数据和软件左上角版本统一提升到`0.8.2`；README、AI接手说明、项目框架和质量门禁同步登记。
- 验证：新增12项模块操作、锁定、逻辑阻断、确定性SVG/哈希和离屏UI测试；完整78项单元/界面测试通过。通过/阻断两张界面图已人工查看，结构图可由XML解析，磁盘Scene/SVG与服务重新生成结果一致。
- 下一步：用户重点审核16种模块是否够用、单主线排序方式、逻辑检查是否过严/过松，以及结构图是否足够支撑售前沟通；确认后先替换为公司模块库，再开发自由画布/局部遮罩和图片Provider。

### 2026-08-24｜实现 0.9.0 OpenAI 设备方案图片 Provider

- 用户选择：图片生成明确采用 OpenAI，不以 ComfyUI 作为第一接入路线。
- 规格与边界：新增 `SPEC_OPENAI_IMAGE_PROVIDER_MVP.md`；`EquipmentScene` 继续作为唯一逻辑源，提交前重新计算门禁和 `sceneHash`，Provider 只读取不可变快照，不能修改模块或工艺关系。
- Provider：新增 `ppt_generator/openai_image.py`，使用官方 Python SDK 的 Image Edit 接口和固定模型快照 `gpt-image-2-2026-04-21`；支持连接测试、1～4 张候选图、尺寸/质量参数、API错误脱敏、PNG校验和原子写入。
- 界面：新增 `ppt_generator/ui/openai_image_dialog.py`；无CAD方案逻辑通过后可打开 OpenAI 弹窗，临时输入或读取 `OPENAI_API_KEY`，异步测试连接和生成，预览候选并记录人工采用。API Key 不写项目或生成记录。
- 可追踪输出：结果保存到 `output/ai_candidates/<sceneHash>/<batchId>/`，`generation.json`记录 Scene 快照、模型、提示词、控制图/候选图哈希和 OpenAI 请求 ID；`accepted.json`只在人工点击采用后生成。
- 版本与依赖：版本提升到 `0.9.0`，增加官方 `openai>=2,<3` 依赖；本机已安装并核对 SDK 2.54.0 的 `images.edit` 参数。
- 验证：新增 Provider 成功、阻断/过期 Scene、密钥缺失、返回数量、损坏图片和密钥脱敏等测试；完整83项单元/界面测试和规定的全量Python编译检查通过，OpenAI弹窗离屏截图已人工检查。
- 下一步：用户提供自己的 OpenAI API Key，在界面生成第一张真实候选图并审核模块数量、顺序、检测与剔除逻辑；当前不自动写入正式方案或 PPT。

### 2026-08-25｜实现 0.9.1 Codex Pro 会员图片 Provider

- 用户纠偏：已经购买 Codex Pro，不接受为了内部软件再次充值 OpenAI API；原 429 `credit balance exhausted` 是旧实现走 API Key 单独计费路径造成的，不应继续作为默认方案。
- 官方能力核对：依据 OpenAI Docs，Codex 支持通过 ChatGPT 登录使用订阅额度，`codex exec`/Codex SDK 可用于脚本化内部工作流；本机已有 `codex-cli 0.149.0`。官方 Python SDK 的独立 127.6 MB 运行时下载在本次环境中长时间无进度，因此 MVP 直接调用已安装的官方 CLI，并把边界保留为可替换 Provider，后续可换 SDK 而不改 Scene 或 UI 流程。
- 规格：新增 `SPEC_CODEX_PRO_IMAGE_PROVIDER_MVP.md`，明确 ChatGPT 登录、API 备用、沙箱、凭据、使用限制和验收边界。
- Provider：新增 `ppt_generator/codex_image.py`；支持运行时发现、ChatGPT 登录状态、浏览器登录、固定 `gpt-5.6-sol` 代理、临时会话、PNG 文件/内嵌结果兼容、线程 ID 提取、中文额度错误和 `codex-image-batch/v1` 追踪记录。
- 安全边界：每次 Codex 任务只在新建 `output/ai_candidates/<sceneHash>/<batchId>/` 内以 `workspace-write` 运行；只附加结构控制 PNG，不把项目源码、API Key、ChatGPT 密码或 OAuth token 交给 Provider。子进程显式移除 `OPENAI_API_KEY` 并强制 `forced_login_method="chatgpt"`。
- 界面：AI 效果图窗口默认“Codex Pro（ChatGPT 登录）”，API Key 默认隐藏；提供“检查登录”和“登录 Codex”，所有操作继续在线程执行。切换“OpenAI API（单独计费）”后才显示 Key；429 余额错误会明确建议切回 Codex Pro，而不是继续充值。
- 兼容：原 OpenAI API Provider、Scene 门禁、控制图、候选预览、人工采用和输出目录结构保留；采用记录升级为通用 `accepted-ai-image/v1` 并记录 provider/model。
- 版本与文档：软件、包和构建版本提升为 `0.9.1`；同步更新 README、AI 接手顺序、项目框架和质量门禁。
- 验证：新增 4 项 Codex 登录、文件候选、内嵌 PNG 和额度错误测试，补充 API 429 中文提示和默认/备用 Provider 界面断言；完整 88 项单元/界面测试及规定的 Python 编译检查通过。真实 Codex 登录和真实 ImageGen 仍需用户在浏览器完成账号授权后验证。
- 下一步：启动软件，在“无CAD方案实验室”保持默认 Codex Pro 模式，完成一次浏览器登录并生成首张真实候选图；若图片能力未返回文件，依据该批次失败信息补充 App Server 事件适配，不退回 API 充值方案。

### 2026-08-25｜实现 0.9.2 整机与模块级结构/提示词/图片绑定

- 用户纠偏：AI生成后的下一步不能只处理一张主设备图；在生成和PPT回写之前，必须先为整机及每个设备模块分别绑定提示词和人工定义结构，再逐个生成、审核和采用图片。
- 规格与顺序：新增`SPEC_MODULE_VISUAL_BINDING.md`，固定流程为“主线逻辑 → 整机/模块结构 → 各自最终提示词 → 各自候选图/人工采用 → 整套同步正式设备方案 → 人工同步PPT”。不再把单张整机图直接写入PPT。
- Scene模型：`EquipmentScene`升级为v2；整机保存附加结构、补充要求、采用图和来源，每个`SceneNode`保存结构JSON、补充要求、采用图和来源。结构字段覆盖内部部件、产品路径、机构/运动关系、视觉部件、安全约束、禁止元素和自定义说明。
- 生成目标：一个Scene确定性生成1个overview目标和N个module目标；每个目标独立拥有目标ID、类型、结构快照、最终提示词、目标哈希和控制SVG。修改单个模块结构只使整机及该模块目标失效，不影响其它模块目标；图片路径和来源不参与结构哈希。
- AI回写：AI窗口按当前选中的整机或模块目标生成；请求快照、批次、`accepted-ai-image/v2`均记录目标ID/类型/哈希。人工采用只回写对应Scene目标，Provider返回错误目标哈希时拒绝候选。
- 正式项目联动：新增`scheme_application.import_no_cad_scene`作为唯一跨模块写入点；按稳定Scene节点ID幂等更新正式设备模块，保留人工创建模块，登记设备图片素材，并写入结构、提示词、图片和来源。主窗口只接收Scene数据事件，不让两个编辑器直接互调；同步结构不会自动生成PPT。
- 正式设备方案：项目schema提升为4；设备总览和每个实体模块可查看绑定结构与提示词。现有`materialize_equipment_scheme`继续负责设备总览/模块页Slot写入，并继续阻止缺少模块图片的正式PPT同步。
- 旧候选边界：`output/ai_candidates/.../20260824T162452484116Z`中的已生成整机图保留为旧版候选记录；因为生成时尚无模块级结构/目标哈希，不静默冒充新结构的已审核图片，也不自动写入PPT。
- 版本与文档：软件、包和构建版本提升为`0.9.2`；同步更新README、AI接手顺序、项目框架、工程验证命令和本记录。
- 验证：新增整机/模块目标独立哈希、Scene往返、模块请求快照、采用记录、项目Application导入/幂等、schema-v4持久化、主窗口联动及最终PPT Slot绑定测试；完整96项单元/界面测试通过，规定的全量Python编译检查通过。
- 下一步：用户在“结构 / 提示词绑定”中先审核演示模块结构字段和每个模块最终提示词；确认后再按目标逐一生成模块图。全部模块采用后，点击“同步结构到正式设备方案”，最后在设备方案页同步到PPT并检查PowerPoint/WPS。

### 2026-08-25｜实现 0.9.3 模块整套绑定随增删联动

- 用户补充：图片、提示词和PPT必须归属于同一个设备模块；在“增加模块”处新增模块时，结构、提示词、图片目标和PPT页面绑定也要同时增加，不能作为互不关联的独立记录。
- 统一绑定单元：以稳定模块ID作为唯一关联键，一个模块固定对应一套“结构定义 + 最终提示词 + 采用图片 + PPT模块页绑定”。增加模块新增一套，删除模块删除对应套件，调整顺序只改变展示和PPT顺序，不拆散原绑定。
- 状态可视化：正式设备方案表扩展为“结构、提示词、图片、PPT绑定、关联流程”等8列；新增按钮改为“增加模块及绑定”，新模块会明确显示待定义、待生成、待添加和默认绑定状态。
- 失效保护：模块结构、顺序或上下文变化后，系统按目标哈希识别并清除过期采用图；正式同步再次校验，禁止把旧结构生成的图片误写入设备方案和PPT。
- PPT联动：通过无CAD方案增加的模块会生成独立模块目标；完整采用并同步后，在正式设备方案中形成一个设备记录，并在PPT中占用一个独立设备模块页。删除和排序沿用同一模块ID联动。
- 验证：新增模块增删/排序整套绑定、结构变化只使对应采用图失效、界面状态列和人工新增绑定行等测试；完整99项单元/界面测试通过，规定的全量Python编译检查通过。

### 2026-08-25｜实现 0.9.4 大尺寸模块效果总览

- 用户参考：依据工业设备方案分区总览图，要求在效果图环节提供一个更大的界面，同时展示整机和各模块生成图，并可点击放大查看。
- 规格：新增`SPEC_MODULE_VISUAL_OVERVIEW.md`，明确总览是只读审核界面，不复制Scene状态、不借用其他模块图片，也不把多图合成为一张不可编辑的PPT图片。
- 界面：无CAD方案顶部增加“模块效果总览”；独立大窗口采用深蓝标题栏的工业分区卡片，按视觉目标顺序自适应1～3列显示整机和全部模块，并汇总目标数、已采用数和待完成数。
- 图片查看：卡片列表使用按显示尺寸解码的缩略图，避免一次加载全部原始像素；点击已采用图片或“查看大图”后才加载原图，支持适应窗口、100%、放大和缩小。
- 联动：总览直接消费当前`NoCadSchemeResult.visual_targets`；模块增加、删除或排序后重新打开/刷新即按稳定模块ID重建卡片，缺图和损坏路径保留独立状态。
- 验证：新增目标/卡片一一对应、模块新增联动、缺图禁用、点击打开正确目标图片及原图缩放的离屏界面测试；完整102项单元/界面测试和规定的全量Python编译检查通过。`output/ui_module_visual_overview_v094.png`为三列布局审核图，重复使用现有旧候选图仅验证卡片版式，不写入Scene或正式PPT。

### 2026-08-25｜实现 0.9.5 AI效果图窗口内选择与预览

- 用户纠偏：整机或设备模块不应在外层结构编辑区决定后再打开生图；应进入“AI设备方案效果图”窗口后选择，同时在该窗口预览和查看对应效果图。
- 目标选择：AI窗口左侧新增“本次生成目标”，列出整机及全部模块并显示待生成、已采用或本次已采用状态；切换后同步更新目标标题、哈希、最终提示词和生成按钮。
- 防串图：切换目标立即清空上一目标的候选批次、候选下拉和采用按钮；AI请求运行期间目标选择被锁定，返回批次仍校验当前目标哈希。
- 效果预览：选择目标后右侧直接预览当前已采用图；生成后显示本批候选图；已有采用图和本次采用图均可在AI窗口点击“查看已采用图”打开原图缩放查看。
- 多目标采用：同一个AI窗口可依次生成并采用多个目标，采用记录按目标ID替换并保留；关闭窗口后外层先统一校验所有目标ID、哈希和文件，再逐项回写Scene，避免部分错误写入。
- 外层入口：按钮统一改为“打开 Codex Pro AI效果图生成”；“结构 / 提示词绑定”中的下拉改名为“结构编辑目标”，只作为打开AI窗口时的初始定位。
- 验证：新增AI窗内目标切换、旧候选清除、已有采用图预览/大图、多个目标统一回写和本次采用记录测试；完整105项单元/界面测试和规定的全量Python编译检查通过。`output/ui_ai_effect_target_selector_v095.png`为目标选择及已有图预览布局审核图，临时复用旧候选图且未写入正式Scene/PPT。

### 2026-08-25｜实现 0.9.6 AI候选图项目绑定与持久化

- 用户要求：生成的候选图必须和对应KY PPT项目绑定，不能只在当前窗口或全局输出目录中临时存在。
- 项目模型：`PptProject`升级到schema v5，新增32位稳定`project_id`、`no_cad_scene`和`ai_image_batches`；schema 1～4项目加载时生成ID，保存后升级为v5。批次`projectId`与项目不一致时拒绝加载。
- 文件隔离：整机和模块候选图统一写入`output/ai_candidates/<projectId>/<targetHash>/<batchId>/`，项目JSON仅保存批次索引和图片路径，不嵌入PNG二进制。
- 历史恢复：AI效果图窗口新增“历史批次”，只显示当前项目ID、目标ID和目标哈希全部匹配的记录；可重新预览和采用，缺失图片会标记且禁止采用。
- 保存联动：新批次立即进入无CAD工作区索引；已保存项目自动原子更新项目文件，未首次保存的新项目提示点击“保存项目”。重新打开项目后恢复Scene、已采用图和候选历史。
- 安全边界：不自动猜测旧版未绑定候选图的归属，不删除旧输出；结构哈希变化后旧批次仍保留追溯，但不进入当前可采用列表。
- 规格与验证：新增`SPEC_PROJECT_AI_IMAGE_PERSISTENCE.md`；覆盖schema迁移、项目异主拒绝、原子保存、目标历史过滤/预览、编辑器事件与已有项目自动保存测试；本项目`tests/`下110项pytest和unittest全部通过，全量Python编译检查通过。
