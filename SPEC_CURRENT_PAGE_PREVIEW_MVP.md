# Spec: 当前页预览 MVP

> 状态：2026-08-24 已实现 0.5.3 横向预览工作区与方案同步选中页恢复。Microsoft PowerPoint 实际导图、加载遮罩、切页预加载和缓存通过，WPS 后端因本机未安装尚待验收。

## Goal

在桌面软件最右侧增加只读的“当前页预览”区域。用户在“方案模块”中选中页面后，软件展示该页面按当前项目数据生成后的实际画面，帮助判断版式、文字和图片是否合适。

## Scope

In scope:

- 主工作区采用左右两栏：左侧为项目编辑，右侧上方为当前页预览、下方为模板与输出/结构记录，不替代原有结构预览和运行记录。
- 选中模块中的页面后自动请求预览；切换页面、修改模块页面字段后自动刷新。
- 预览基于项目最终渲染结果，因此包含项目公共值、模块值和页面覆盖值。
- Windows 下优先调用 Microsoft PowerPoint 导出当前页 PNG；不可用时尝试 WPS 演示。
- 导图在独立后台进程运行并设置超时，避免 Office 卡住主界面。
- 提供手动刷新按钮、当前模块/页面信息、加载中和失败说明。

Out of scope:

- 在预览画面中拖动、缩放或直接编辑 PPT 对象。
- 动画、视频、音频和切换效果播放。
- 无 PowerPoint/WPS 环境下自行实现完整 PPT 排版引擎。
- 连续缩略图列表和整份 PPT 放映。

## Inputs And Outputs

Inputs:

- 当前 `PptProject` 快照。
- 当前模块 UUID 和页面 UUID。
- 页面或字段的用户操作。

Outputs:

- 右侧上方区域显示一张保持源模板横向比例的 PNG 预览图。
- UI 状态：未选择、生成中、成功、Office 后端不可用或超时。

## Behavior

- 正常路径：选中页面 → 后台生成临时项目 PPTX → 定位最终物理页码 → PowerPoint/WPS 导出 PNG → UI 缩放显示。
- 连续切换：同一时刻只运行一个任务；运行中发生的新选择在任务结束后刷新最新页面。
- 选择模块：清空图片并提示选择模块中的具体页面。
- 页面被禁用模块包含：仍可选中，但不属于最终 PPT，提示需启用模块后预览。
- Office 缺失或异常：显示明确错误，不影响项目编辑和正式 PPTX 生成。
- 临时文件：任务结束后清理；不覆盖原始模板和正式输出文件。
- 兼容性：PowerPoint 与 WPS 都通过独立适配入口；首版以本机可用后端为准。

## Risk

- PowerPoint/WPS 首次启动弹窗可能导致自动化超时；必须在子进程设置超时，不能冻结 UI。
- 每次生成需要渲染临时 PPT，复杂项目可能需要数秒；使用短延迟合并连续操作。
- Office 导出结果最接近实际显示，但不同 Office 版本和字体环境仍可能有差异。
- 不允许结束用户已经打开的 PowerPoint/WPS 进程。

## Acceptance Tests

- 给定启用模块中的页面，能计算其在最终 PPT 中的 1 基物理页码。
- 给定禁用模块页面，预览服务拒绝生成并返回明确错误。
- 选中页面后，预览栏记录正确模块/页面并进入加载或测试禁用状态。
- 后台导出失败不会导致主窗口崩溃，用户仍可继续编辑。
- 完整单元测试与 Python 编译检查通过。
- 使用 NAT6704 模板在本机 Office 后端导出一张真实 PNG，并人工检查截图；如 Office 首启阻塞，则记录替代验证和剩余风险。

## Implementation Notes

- 新增 `ppt_generator/preview.py`：物理页码定位、临时渲染、后端调用。
- 新增 `ppt_generator/office_preview.py`：独立进程中的 PowerPoint/WPS 单页导图。
- 新增 `ppt_generator/ui/slide_preview.py`：只读预览控件。
- `ModuleEditor` 只发出当前模块/页面 ID，不负责渲染。
- `MainWindow` 负责防抖、后台任务和最新选择协调。

## 0.5.1 性能优化

### Goal

- 避免每次切页都重新渲染整份PPT和重新启动 PowerPoint/WPS。
- 新页面图片尚未完成时保留旧画面，并明确标注“正在加载新页”，不让右侧区域突然变空。

### Behavior

- 临时PPTX只包含当前目标页面，但页码、模块编号和字段值仍取自完整项目的 `SlideContext`。
- Office预览子进程在软件运行期间复用同一个 PowerPoint/WPS 实例；出现超时或错误时销毁该独立进程，下次自动重建。
- 页面PNG按模板、目标页面和当前 `SlideContext` 内容计算指纹并做内存缓存；未修改时再次选择立即显示。
- 手动点击“刷新”绕过缓存；页面字段改变后因指纹变化自动生成新图。
- 加载新图时可以继续操作软件；当前图片作为旧画面暂留，生成完成后一次替换。

### Acceptance Tests

- 定向预览PPTX仅包含1页，目标内容与完整项目对应页面一致，且原模板哈希不变。
- 同一页面内容未变化时命中缓存，不启动第二个后台渲染任务。
- 首次Office启动后，连续预览两个不同页面复用同一后端进程。
- Microsoft PowerPoint本机实测记录冷启动、暖切页和缓存命中耗时；暖切页应明显快于0.5.0基线。

## 0.5.2 两级预览与预加载

### Goal

- 用户切换页面后立即看到该模板页的静态缩略图，不再把上一页画面误认为当前页。
- 实时内容尚未完成时，用明显遮罩说明当前状态；完成后再替换为最终数据画面。
- 用户按顺序浏览时，上一页和下一页尽可能已经在后台生成完成。

### Behavior

- 第一级：首次访问某个模板页时按需导出该页PNG，并按模板SHA-256持久缓存；不在启动时批量导出整套模板。
- 第二级：选中页面后继续执行0.5.1单页实时渲染；完成后覆盖第一级缩略图。
- 缩略图生成中或首次尚无缩略图时，可以暂时保留旧图，但画面中央必须显示“正在加载目标页面，当前为上一页”。
- 已有模板缩略图时立即切换到正确的模板页面，并显示“模板预览，实时内容加载中”遮罩。
- 当前实时页面完成后，在无用户新请求时依次预生成最终结构中的上一页和下一页；预加载结果进入同一内存缓存。
- 用户新选择优先于相邻页预加载；不启动并行Office实例。
- 模板缩略图缓存位置为项目 `output/.preview_cache/<template_sha256>/`，不写入原模板。
- 原模板为中文文件名时，单页导图先复制到临时英文文件名，规避部分PowerPoint版本的 `0x80070002` 错误；临时文件任务后清理。

### Acceptance Tests

- 首次访问某个来源模板页时只导出该页的有效PNG，不让整套模板导出阻塞启动。
- 该来源页已有有效持久缓存时不再调用Office导出。
- 选择未实时缓存的页面后，先显示对应来源模板页并出现加载遮罩；实时结果返回后遮罩消失。
- 当前页面完成后，前后相邻页面进入预加载队列；返回刚看过或已预加载页面时命中内存缓存。
- 用户快速切换时只显示最新选择，Office进程数量不增加。

## 0.5.3 横向预览工作区与选中页恢复

### Goal

- 让横向矩形 PPT 在编辑过程中获得足够宽度，不再被第三个窄列压缩。
- 模板与输出仍位于当前项目右方，但调整到当前页预览下方，并能随窗口尺寸自适应。
- 修改检测流程或设备模块并执行“同步到正式PPT”后，恢复到同一模块的同一页序位置，避免因页面 UUID 重建而清空预览。

### Layout

- 主工作区只保留左右两列：左侧“当前项目 + 功能页签”，右侧为纵向审阅区。
- 右侧审阅区上方固定为“当前页预览”，优先获得纵向空间；画布最小尺寸按横向页面设置为 `480 × 270 px`。
- 右侧审阅区下方使用页签自适应收纳：第一页为“模板与输出”，包含模板、输出、导航样式和项目统计；第二页为“结构与记录”，并排展示PPT结构和运行记录。
- 窗口放大时优先扩展右侧与预览区；窗口缩小时下方工具区通过页签避免纵向控件互相挤压。

### Behavior

- 设备方案同步前记录当前模块和页序号；同步生成的新页面即使 UUID 改变，也恢复到该模块相同页序号。
- 原页序号超出新页数时选择该模块最后一页；模块没有页面时才选择模块节点并显示“请选择具体页面”。
- 选中页恢复后沿用现有防抖、两级预览、缓存和相邻页预加载逻辑。

### Acceptance Tests

- 主工作区为两个水平子区；右侧审阅区为“上预览、下工具”的垂直子区。
- 当前页预览画布最小尺寸不小于 `480 × 270 px`，缩放时仍保持图片比例。
- 下方工具区存在“模板与输出”和“结构与记录”两个页签，原有控件和功能均可访问。
- 检测流程同步重建页面后，当前模块与页序号保持，预览选择切换到新生成页面 UUID，而不是退回模块节点。
- 完整单元测试与 Python 编译检查通过。
