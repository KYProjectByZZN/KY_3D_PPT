# Spec：设备介绍 PPT 模块方案

> 状态：2026-08-24 第三轮修改已实现，待用户视觉审核。导航字体统一加粗并由 `8.8 pt` 提升至 `10 pt`，软件已增加导航背景色选择；检测模块页已收敛为“检测模块图 + 工位编号/名称”，不展示检测效果，也不展开逐工位光学参数。

## 1. Goal

把“设备介绍”从当前不统一的图文说明页，升级为适合工业自动化、机器视觉和非标设备技术方案的结构化页面模块：

- 图片为主，文字为辅。
- 一页只回答一个问题。
- 页面风格统一，可人工调整，也可按设备模块数量自动扩页。
- 输出为可编辑 PPTX，并兼容 Microsoft PowerPoint 和 WPS。
- 内容来源可追踪，AI 只能建议，正式输出以前必须人工确认。

## 2. 设计判断

### 2.1 不直接照搬提示词的地方

1. “设备总览页”和“设备模块总览页”可能重复。模块少于 3 个、或只有同一张整机图时，默认合并，不强行生成两页。
2. “四种版式”应理解为四种页面职责，不应让每个模块任意选择完全不同的页面风格，否则整章会失去一致性。
3. 当前第 6～9 页的版式、图文比例和文字结构不一致，不宜继续作为最终标准模板；新方案只保留其内容价值，重新建立统一页面蓝图。
4. CAD、三维模型和设备总图默认使用完整适配 `contain`，不能继续默认裁切，否则可能把机构边界、标注或安全护罩切掉。
5. 完整设备参数继续放在现有“设备参数”章节；设备介绍页只挑选 4～6 个决策指标，避免重复。
6. 首版样本的大面积圆角卡片、胶囊标签和浅色容器偏软件界面风格，不适合作为工业技术方案标准；二版改为直角图片、细分隔线、线性引线和传统图题。
7. 检测模块介绍与检测效果必须彻底分开：设备介绍章节只展示检测模块结构图，以及包含几个检测工位、每个工位叫什么；OK/NG、缺陷样图和检测参数继续由检测效果/设备参数章节负责。

### 2.2 三个页面层级

```text
设备级：这是什么设备、整体能力是什么
  ↓
模块级：设备由哪些功能模块组成
  ↓
细节级：关键模块具体如何实现、有哪些重要结构
```

“检测流程”负责解释产品如何从输入走到输出；“设备介绍”负责解释设备由什么组成；“检测效果/视觉系统”负责证明检测能力。三部分不重复堆叠内容。

## 3. Scope

### In scope

- 设备总览页、模块分区页、单模块介绍页、模块细节页四种页面角色。
- 内容页统一章节导航栏、当前章节高亮、可调导航高度和可选背景色。
- 设备和模块的结构化字段、图片分类、自动扩页、编号和内容容量规则。
- 模块数量变化后自动复制、删除和重新编号页面。
- PowerPoint 原生文字、卡片、编号和标注；导出后仍可编辑。
- 预览时允许缺失字段占位；正式输出时执行完整性检查。

### Out of scope

- 自动识别任意 CAD/三维截图中的机械机构。
- 自动判断机械方案是否可行、节拍是否满足或安全设计是否合规。
- Visio 式自由画布、复杂引线避让和自动布线。
- 把完整设备参数表、视觉选型表或检测效果重复塞进设备介绍页。
- AI 未经人工确认直接写入正式技术承诺。

## 4. 页面结构

| 页面角色 | 是否必选 | 数量规则 | 核心问题 |
|---|---:|---:|---|
| A. 设备总览 | 必选 | 固定 1 页 | 这是什么设备，核心能力是什么 |
| B. 模块分区 | 条件生成 | 固定 0～1 页 | 设备由哪些模块组成 |
| C. 单模块介绍 | 必选 | 每个启用模块 1 页 | 该模块做什么、怎么工作 |
| D. 模块细节 | 按需 | 每模块 0～1 页 | 关键结构和设计细节是什么 |
| E. 检测模块介绍 | 检测模块必选 | 固定 1 页 | 检测模块长什么样，包含哪些检测工位 |

建议章节顺序：

```text
3.1  设备总体方案
3.2  功能模块分区（满足生成条件时出现）
3.2.1  上料与定位模块
3.2.2  搬运与翻转模块
3.2.3  分类下料模块
3.3  检测模块介绍
```

普通模块细节页沿用同一模块编号，例如 `3.2.2 搬运与翻转模块｜结构细节`，不增加难维护的四级编号。检测模块首版只生成一张模块介绍页，不按工位自动扩展设备介绍页。

## 5. 各页面方案

### A. 设备总览页

目的：让客户在 10 秒内理解设备用途、整体形态和主要能力。

- 左侧 62%～65%：整机等轴测图、正视图或高质量设备照片。
- 右侧 35%～38%：设备名称、一句话定位、4～6 个核心指标。
- 指标采用 2 列卡片，不放完整参数表。
- 可显示的典型指标：设备尺寸、工位数量、生产节拍、上料方式、检测覆盖、动力需求；具体项目按已确认数据选择。
- 如果存在独立模块分区页，本页不再放复杂模块标注。

内容容量：

- 设备名称：不超过 20 个汉字。
- 一句话定位：不超过 45 个汉字。
- 核心指标：4～6 项，每项由短名称、数值和单位组成。
- 主图：1 张，默认完整适配，不裁切。

### B. 模块分区页

目的：建立整机与功能模块之间的对应关系。

- 左侧 68%～72%：整机图或俯视布局图。
- 图上使用派生编号 `01、02、03……` 标注模块位置。
- 右侧 28%～32%：模块编号、名称和一句话功能，最多 8 个模块。
- 引线和编号采用 PowerPoint 原生图形，便于后续移动。

生成条件：

- 启用模块不少于 3 个时默认生成。
- 模块少于 3 个时默认合并到设备总览页，用户可强制保留。
- 第一阶段不自动识别 CAD；模块标注位置由工程师提供的标注图，或由用户在软件中手动设置归一化坐标。

### C. 单模块介绍页

目的：说明一个模块的作用、组成和动作逻辑，是设备介绍章节的核心重复页。

- 左侧 60%～65%：模块主图或局部三维图。
- 右侧 35%～40%：
  - 模块名称和一句话定位；
  - 最多 3 个核心功能；
  - 4～6 个关键配置/参数。
- 底部：3～5 步动作链，例如 `接收产品 → 定位 → 执行动作 → 信号确认 → 输出`。
- 与检测流程关联的工位名称可作为小标签显示，不写成长段落。

现有“对应工位、模块功能、动作过程、关联流程”四行长文本需要拆分为卡片和步骤，不继续直接拼接成大段项目符号。

### D. 模块细节页

目的：只展示真正需要解释的关键机械、视觉或维护细节。

- 左侧约 60%：模块整体结构图。
- 右侧约 40%：2～3 张局部放大图。
- 每张局部图只配 1 个标题和 1 句说明。
- 可用于展示夹具定位、翻转机构、光学成像位置、快换结构、安全防护或维护空间。
- 少于 2 张有效细节图时默认不生成，避免出现空洞页面。

### E. 检测模块介绍

目的：让客户看懂检测模块在设备中的结构位置，以及模块包含几个工位。

- 检测模块启用时固定生成 1 页，不因工位数量增加页面。
- 主体使用工程师提供的检测模块 CAD、三维图或现场图，不能用检测效果截图代替模块图。
- 在模块图上或右侧列表使用 `01、02、03……` 标明工位；每个工位只写“工位编号 + 工位名称”，例如 `01 俯视检测工位`。
- 本页不展示产品OK/NG图、缺陷样图、检测范围、相机、镜头、光源、触发、节拍或判定结果。
- 视角可包含在人工确认的工位名称中，但不单独展开成参数区。

### F. 检测工位详页（后续可选，不属于当前首版）

如以后确实需要讲解单工位机构，再由用户显式启用工位详页；当前版本不自动生成，避免把设备介绍做成检测效果或光学配置说明。

## 6. 统一视觉规范

### 6.1 配色

| 用途 | 色值 | 说明 |
|---|---|---|
| 顶部品牌导航 | `#C90000` | 沿用现有模板红色，仅用于导航和品牌识别 |
| 正文主文字 | `#222A33` | 标题和主要内容 |
| 次要文字 | `#66727E` | 图注、单位和辅助说明 |
| 技术强调色 | `#315F7D` | 编号、参数值、模块标注 |
| 浅背景卡片 | `#F4F7F9` | 参数和功能卡片 |
| 边框/分隔线 | `#D9E0E6` | 低对比度边界 |
| 警告信息 | `#B3261E` | 仅用于真实风险和异常，不作为普通装饰色 |

正文不使用大面积红色、渐变色、彩色粗边框和重阴影。设备图区域以白色或极浅灰为主。

### 6.2 字体与间距

- 中文、英文、数字统一字体：`Microsoft YaHei` / 微软雅黑；所有文本 Run 同时写入 Latin、East Asian 和 Complex Script 字体声明，避免 PowerPoint/WPS 回退成不同字体。
- 页面标题：20～24 pt。
- 设备/模块名称：18～22 pt。
- 功能和参数：11～14 pt。
- 图注：9～10 pt。
- 正文最小字号不低于 10 pt；内容超限时提示修改，不通过无限缩小字号解决。
- 页面边距、标题位置、图片安全区和分隔线基线在所有页面中保持一致。
- 不使用圆角图片蒙版、胶囊标签、重阴影和大面积浅色卡片；参数区使用直角表格、细分隔线或无边界对齐。

### 6.3 图片规则

- 分类：整机图、模块分区图、模块主图、模块细节图。
- CAD/三维图默认 `contain`；现场照片可选择 `cover`。
- 建议长边不少于 1600 像素；低分辨率只警告，不自动放大冒充高清图。
- 支持透明 PNG，背景统一为白色或浅灰色。
- 正式输出不得回退使用模板样图；缺少模块主图时阻止正式生成。
- 每张正式图片下方必须有可编辑图题，格式为 `图 3.1 设备总体图`；图号由章节结构统一派生，不人工写死。
- 同页多图必须同基线、同类图同尺寸；局部图分别编号，不能依靠圆角外框或悬浮标签区分。

### 6.4 顶部章节导航栏

- 内容页统一显示 `企业简介 / 工艺分析 / 设备布局 / 模块介绍 / 供货范围` 五个一级章节；当前章节高亮。
- 现有模板导航高度约 `0.892 in`，占用内容空间偏多；新版默认高度为 `0.52 in`，允许配置范围 `0.42～0.72 in`。
- 当前章节不再使用整块高色块，改为红色文字加底部细线；其它章节使用深灰文字和浅灰分隔线。
- 五个章节文字统一加粗，字号由首版 `8.8 pt` 提升一级至 `10 pt`。
- 导航背景色默认 `#FFFFFF`，在软件“导航样式”区域可选择任意 `#RRGGBB` 颜色；深色背景自动切换为高对比文字和分隔线。
- 企业Logo高度不超过导航栏高度的 60%，并保持原始比例，不拉伸。
- 页面标题、标题分隔线和正文起始位置均由导航高度派生，调整高度后不得与导航栏重叠。
- 封面、结束页和附件页可按模板配置关闭导航；普通内容页默认启用。
- 样页脚本提供 `--nav-height` 和 `--nav-background` 参数；正式项目将高度、背景色保存在项目样式配置中，正式导出与当前页预览读取同一配置。

## 7. 结构化数据

### 7.1 设备级字段

```text
equipment_name           设备名称
equipment_summary        一句话定位
overview_image           整机主图
module_map_image         模块分区图，可选
core_metrics[]           4～6个名称/数值/单位
force_module_map_page    是否强制生成模块分区页
navigation_height        顶部导航高度，模板级字段
navigation_background    顶部导航背景色，#RRGGBB
```

### 7.2 模块级字段

```text
id                       稳定UUID
name                     模块名称
module_type              上料/定位/搬运/检测/下料等
station                  对应工位
summary                  一句话定位
main_image               模块主图
functions[]              最多3项核心功能
config_items[]           4～6项配置或参数
workflow_steps[]         3～5个动作步骤
detail_images[]          0～3张细节图及说明
callout_position         模块分区图上的可选坐标
detail_page_enabled      是否生成细节页
enabled                  是否进入正式PPT
```

### 7.3 检测模块字段

```text
id                       稳定UUID
detection_module_image   检测模块CAD、三维图或现场图
stations[]               工位列表
station_number           派生工位编号
station_name             人工确认的工位名称，可包含俯视/仰视/侧视
marker_position          工位在模块图上的标注位置，可选
```

数据来源优先级：人工确认值 > 已确认导入值 > Excel/FAR 映射值 > AI 建议值。AI 不覆盖人工确认内容。

## 8. 自动生成规则

1. 有设备名称和整机图时生成设备总览页。
2. 模块数不少于 3 个时默认生成模块分区页；少于 3 个时默认合并。
3. 每个非检测类启用模块生成 1 张单模块介绍页；检测类模块固定生成 1 张检测模块介绍页。
4. 模块有至少 2 张有效细节图且启用细节页时，再生成 1 张模块细节页。
5. 模块顺序决定显示顺序和派生编号；调整顺序后编号、图题和分区图图例同步更新。
6. 页面标题、小标题和图号均由章节结构派生，不写死在模块数据中。
7. 功能超过 3 项、配置超过 6 项或动作步骤超过 5 项时，软件提示精简；不静默截断，也不自动缩成小字。
8. 预览允许使用“待补充”占位；正式导出时至少要求整机主图，以及每个启用模块的名称、摘要和主图。
9. 重复同步只重建设备介绍章节，不覆盖检测效果、设备参数等其他模块。
10. 检测模块介绍页只读取检测模块图和启用工位名称；不读取或复制检测效果图片。
11. 图片变更、工位排序或模块排序后，页面标题、工位号和所有图题同步重算。

示例：4 个启用模块，其中 2 个模块有细节页：

```text
1页设备总览 + 1页模块分区 + 3页普通模块介绍 + 2页普通模块细节
+ 1页检测模块介绍 = 8页
```

## 9. 软件界面方案

设备方案编辑区增加四组结构化输入：

1. **设备总览**：设备名称、定位说明、整机图、核心指标列表。
2. **模块分区**：分区图、模块编号预览、手动标注位置、是否生成该页。
3. **设备模块**：
   - 基本信息：名称、类型、工位、摘要、主图；
   - 核心功能：最多 3 行，可排序；
   - 配置参数：名称/数值/单位列表，最多 6 行；
   - 动作步骤：最多 5 步，可排序；
   - 细节图片：最多 3 张，包含标题和一句说明；
   - 页面开关：是否启用、是否生成细节页。
4. **检测模块**：检测模块图，以及可排序的工位名称列表；工位编号自动派生。

模板与输出区域下方增加“导航样式”：

- 高度：`0.42～0.72 in`，默认 `0.52 in`；
- 背景色：颜色选择按钮并显示十六进制色值；
- 修改后项目文件持久化，正式生成和当前页预览同时生效。

界面继续只编辑结构化数据，不做 PowerPoint 自由画布。右侧当前页预览用于检查最终版式。

## 10. 兼容与迁移

- 当前项目为 schema v3；正式实现时建议升级为 schema v4，并提供 v3 到 v4 的兼容迁移。
- 旧字段 `function` 可迁移为摘要或第一项核心功能；旧字段 `action` 保留为待人工拆分的动作说明，不能未经确认自动切成承诺性步骤。
- 旧第 6～9 页模板仍可读取旧项目，但新项目默认使用统一的模块介绍页和模块细节页蓝图。
- 模板复制继续使用现有 relationship-safe Renderer，不新建第二套PPT生成器。

## 11. Risk

- 机械可行性：PPT只整理工程师确认的方案，不对机构、节拍和安全性自动背书。
- 图片误导：模块主图缺失时不得使用模板样图代替。
- 内容重复：高层指标可引用设备参数数据，但不复制完整参数表。
- 文字溢出：达到容量上限时阻止正式生成并提示修改，不静默截断。
- 标注位置：自动识别任意CAD不在第一阶段，避免错误标注模块。
- 兼容性：需要同时在Microsoft PowerPoint和WPS检查打开、编辑、另存和图片缩放方式。

## 12. Acceptance Tests

- Given 4 个启用模块，其中 2 个有至少 2 张细节图，when 同步设备介绍，then 生成 1 页总览、1 页分区、4 页介绍和 2 页细节。
- Given 2 个启用模块且未强制分区页，when 同步，then 不单独生成模块分区页。
- Given 模块顺序发生变化，when 重新同步，then 页面顺序、模块编号、标题和图例一致更新。
- Given 模块主图缺失，when 正式导出，then 明确阻止导出且不使用模板样图。
- Given 功能、参数或步骤超出容量，when 正式导出，then 给出可理解的精简提示，不截断文字。
- Given CAD图片比例与图片框不同，when 生成，then 默认完整显示设备边界，不裁掉关键结构。
- Given 检测模块有 3 个工位，when 同步设备介绍，then 只生成 1 页检测模块介绍，并列出3个工位名称。
- Given FAR 中存在OK/NG或缺陷图片，when 生成检测模块介绍页，then 不使用这些检测效果图片，也不显示光学参数。
- Given 页面包含中文、英文和数字，when 生成，then 所有可编辑文本统一声明 Microsoft YaHei，不发生字体回退混用。
- Given 页面包含设备图或工位图，when 生成，then 图片无圆角蒙版且下方存在派生图号和图题。
- Given 导航高度从 `0.52 in` 调整为允许范围内其它值，when 重新生成，then 五个章节、当前章节高亮、Logo、页面标题和正文均不重叠且整体同步移动。
- Given 导航高度小于 `0.42 in` 或大于 `0.72 in`，when 生成，then 明确拒绝并提示允许范围。
- Given 用户在软件选择导航背景色，when 保存、重开项目并生成或预览，then 色值保持且作用于所有带章节导航的内容页。
- 输出PPTX具有ZIP签名和必要包成员，所有文字、参数卡片、编号和标注均可编辑。
- Microsoft PowerPoint与WPS打开、编辑、另存均无修复提示，页面布局一致。

## 13. 建议实施顺序

1. 已审核 `output/设备介绍_工业版式参考与工位页方案_v1.pptx`，确认工业版式方向。
2. 已按确认方向更新 `output/设备介绍_工业版五页样本_v2.pptx`：重做 A～D 四张样页，第5页为检测模块图和工位名称列表。
3. 已将导航高度、背景色接入项目保存、PySide6界面、当前页预览和正式Renderer；导航字体统一为加粗10 pt。
4. 用户审核紧凑导航栏、背景色选择、图片比例、图题和检测模块页信息密度。
5. 后续升级设备介绍结构化数据模型和模块编辑界面。
6. 配置新页面蓝图和 Slot，接入自动扩页、工位排序与图号重算。
7. 完成自动化测试，并进行 PowerPoint/WPS 实机验收。

本轮未覆盖正式模板；已以向后兼容字段扩展schema v3项目文件，并将导航样式接入桌面界面、正式Renderer与当前页预览。设备介绍页面蓝图和检测模块结构化编辑仍待用户确认后接入。
