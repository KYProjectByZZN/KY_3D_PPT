# Spec: 通用 Excel → PPT 映射工作台

> 状态：2026-08-23 通用闭环已实现；等待真实业务样例校准字段别名和自动规则。

## Goal

用户可以在桌面软件中预览 Excel，选择单元格或区域，将其映射到 PPT 的文字或表格 Slot，保存规则并重复应用，不依赖程序预先知道某一种公司 Excel 模板。

## Scope

In scope:

- 打开 `.xlsx/.xlsm`，选择工作表并以表格方式预览数据。
- 自动建议可能的表头行。
- 选择一个单元格、列段或矩形区域，映射到 PPT 文字/表格 Slot。
- 文字区域按行展开；表格区域保持二维结构。
- 保守扫描“字段名—右侧值”结构，建议文字映射，结果必须由用户确认。
- 映射规则启用/停用、删除、保存、加载和随项目持久化。
- 应用映射后写入当前项目数据，再复用现有 PPT 生成器。
- 显示范围、目标、写入方式和预览值，并报告无效工作表/范围/尺寸。

Out of scope:

- 在没有真实样例时承诺自动理解客户、设备参数、检测项等业务语义。
- 读取旧版 `.xls`、受密码保护工作簿、VBA宏执行、Power Query 和外部数据刷新。
- 修改或覆盖用户原始 Excel。
- 本阶段自动提取 Excel 内嵌图片；图片仍从图片素材工作区导入。

## Inputs And Outputs

Inputs:

- Excel 路径、工作表、A1范围、目标 PPT Slot、写入模式和分隔符。
- 已加载的 PPT 模板配置。

Outputs:

- 项目中的 `excel_mappings` 规则列表。
- 应用后的文字字符串或二维表格数据。
- 可独立保存和加载的映射规则 JSON。

## Behavior

- 原 Excel 以 `read_only=True` 和 `data_only=True` 读取，不写入源文件。
- 单个单元格映射到文字；多单元格映射到文字时按行拼接；表格 Slot 接收完整二维区域。
- 表格范围尺寸必须与模板 Slot 的 `rows/columns` 一致，否则拒绝添加或应用。
- 自动建议只扫描前100行和前20列，并以模板 Slot 的显示名/key进行保守匹配。
- 同一个 PPT Slot 最多保留一条启用映射；新增时替换旧规则需明确处理。
- 缺失、公式空值、范围越界和目标不存在时给出具体警告，不静默忽略。

## Risk

- `data_only=True` 依赖 Excel 已保存的公式缓存；无缓存公式可能读为空。
- 合并单元格只有左上角有值，通用映射不会自动填充合并区域。
- 大工作簿需要限制界面预览范围，实际应用规则仍从源文件读取目标范围。
- 无业务样例的自动建议只能降低人工配置量，不能替代最终字段确认。

## Acceptance Tests

- [x] 测试工作簿的工作表、行列和中文内容能加载到预览模型。
- [x] 表头识别在典型表格中返回正确行号。
- [x] `B2 → project_title` 能生成文字值。
- [x] `A6:D12 → equipment_parameters` 能生成 7×4 二维表格。
- [x] 尺寸不匹配、未知工作表/目标和无效范围会返回明确错误。
- [x] 映射规则随 `*.kyppt.json` 保存并恢复。
- [x] UI 可选择区域、添加/删除/启用规则并应用到项目字段。
- [x] 应用5条演示映射后，真实 NAT6704 输出为有效23页PPTX，封面及两张表内容正确。
- [ ] 使用用户真实Excel校准字段别名、表头策略和多模板识别。

## Implementation Notes

- 新增 `ppt_generator/excel_mapper.py`，保持核心不依赖 PySide6。
- 扩展 `PptProject`，兼容已保存但没有 Excel 字段的旧项目 JSON。
- Excel 页签使用 `QTableWidget` 预览，不引入 Qt WebEngine。
- 真实样例到位后，在通用规则之上增加模板指纹和业务字段别名，不改写映射核心。
