# 模块级结构、提示词与图片绑定规格

## 1. 目标与范围

在“无 CAD 逻辑方案”中建立可追踪的一对多视觉生成计划。一个设备方案包含一个整机目标和多个设备模块目标；每个目标分别拥有结构定义、补充要求、确定性最终提示词、人工采用图片及来源记录。只有整套结构同步到正式设备方案后，才允许通过现有设备方案功能生成 PPT 页面。

本轮不实现 CAD、机械尺寸计算、多 Agent 自动设计，也不把历史整机候选图静默写入 PPT。

## 2. 状态所有权

- `EquipmentScene` 是无 CAD 实验阶段唯一状态所有者。
- `SceneNode` 拥有该模块的自定义结构、补充提示要求、采用图片与来源记录。
- `NoCadSchemeResult` 只保存由 Scene 确定性计算出的生成目标，不反向修改 Scene。
- `PptProject.equipment_scheme` 是正式设备方案的唯一状态所有者。
- 从 Scene 到正式项目的跨模块写入仅由应用服务完成；UI 不直接互相调用。

## 3. 输入与输出

输入：

- 整机项目名、产品名、主线节点与空间关系；
- 每个模块的结构化 JSON；
- 整机及模块的人工补充提示要求；
- AI Provider 返回并经人工采用的候选图。

输出：

- 一个 `overview` 整机生成目标；
- 每个 Scene 节点一个 `module` 生成目标；
- 每个目标稳定的目标 ID、目标哈希、结构快照、最终提示词和控制 SVG；
- 正式设备方案中的整机/模块结构、提示词、图片与来源记录；
- 由现有 `materialize_equipment_scheme` 写入设备总览及设备模块 PPT 图片位。

## 4. 行为规则

1. 整机和模块提示词分别生成，不允许用整机提示词代替模块提示词。
2. 模块结构至少包含内部部件、产品路径、机构关系、运动关系、视觉部件、安全约束、禁止元素和自定义说明。
3. 模块结构 JSON 必须是对象；列表字段必须是字符串数组。
4. 修改结构、节点关系或补充提示要求后，目标哈希必须改变，旧图片不得冒充新结构的审核结果。
5. 图片路径和来源记录不参与结构哈希，采用图片本身不会导致目标失效。
6. 整机或模块目标必须在“AI设备方案效果图”窗口内选择；外层结构编辑目标仅决定初始选中项，不限制本次AI窗口只能生成该目标。
7. AI窗口每次生成仍只针对当前选中的一个目标；切换目标时必须清空上一目标的候选预览，禁止跨目标采用旧批次。
8. 同一AI窗口可依次为多个目标生成并人工采用；关闭窗口后由外层统一按目标ID和目标哈希校验，再分别回写对应Scene目标。
9. AI窗口选择目标后，右侧优先预览该目标当前已采用效果图；生成后显示本批候选图，已采用图和本次采用图均可在窗口内打开大图查看。
10. 同步到正式设备方案是显式人工动作；同步结构不自动生成 PPT。
11. 再次同步时按 Scene 节点 ID 更新既有导入模块，保留手工创建的非 Scene 模块。
12. 正式 PPT 同步继续要求所有启用设备模块存在有效图片，防止模板样图混入输出。
13. 新增一个Scene模块时，必须立即新增且只新增一个模块视觉目标；该目标拥有自己的结构、提示词、图片位和稳定模块ID。
14. 删除模块时一并删除其视觉目标；模块排序只改变顺序和上下游关系，不允许结构、提示词、图片绑定到别的模块ID。
15. 同步到正式设备方案后，每个启用模块对应一个设备模块记录和一个PPT模块页面；模块数增加或减少时，最终PPT模块页数同步变化。

## 5. 失败与恢复

- 非法结构 JSON：阻止应用，保留旧结构并在界面提示。
- Scene 逻辑门禁未通过：禁止 AI 生成和正式同步。
- 候选图不存在、哈希不匹配或目标已变化：不得绑定。
- 项目模板缺少设备模块图片位：沿用现有 `SchemeError`，不得猜测写入位置。
- Provider 失败：保留 Scene、提示词和已完成批次，不修改正式项目。

## 6. 验收标准

- 演示 Scene 产生 1 个整机目标和与节点数相同的模块目标。
- 每个模块目标的结构、提示词、目标哈希互相独立且可序列化。
- 修改一个模块结构只改变整机目标及该模块目标，不改变无关模块目标。
- AI 对话框的清单、控制图、请求清单和 `accepted.json` 都记录目标 ID/类型/哈希。
- AI 对话框内可直接选择整机或任一模块；切换后目标标题、哈希、提示词和控制图同步变化，旧候选不可继续采用。
- AI 对话框内能预览当前目标已有采用图，并能打开原图查看；没有采用图时显示独立缺图提示。
- 同一对话框依次采用多个目标时，关闭后各图片只回写其目标ID对应的Scene字段。
- 人工采用模块图片后只更新对应 Scene 节点。
- 新增/删除/排序模块后，视觉目标数量、目标顺序和模块ID绑定同步更新且不串位。
- 显式同步后，正式设备方案可查看整机和每个模块的结构、提示词、图片来源，并按既有页面版式生成 PPT。
- 项目文件可向后读取 schema 1～3，并以 schema 4 保存。
- 全量单元测试与 Python 编译检查通过。

## 7. 实施说明

- 保留现有 `OpenAIImageProvider` 与 `CodexImageProvider` 边界，仅扩展请求目标信息。
- 保留现有 `EquipmentScene -> SVG -> Prompt -> Provider` 链路，新增目标选择，不另建重复 AI 系统。
- 保留现有 `EquipmentScheme -> materialize_equipment_scheme -> PPT Slot` 链路，新增纯应用服务完成 Scene 导入。
