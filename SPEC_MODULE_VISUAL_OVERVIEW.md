# Spec: 模块效果图总览与大图查看

## Goal

在无CAD方案实验室中增加一个大尺寸的效果图审核界面，参考工业方案总览图的分区卡片结构，一次展示整机和所有设备模块的已采用效果图，并支持点击任意已采用图片打开大图查看。

## Scope

In scope:

- 在无CAD方案编辑器顶部增加“模块效果总览”入口。
- 使用独立大窗口按模块顺序展示一个整机卡片和每个模块各一个卡片。
- 每张卡片显示顺序、目标名称、采用状态和缩略图。
- 缺少采用图时显示明确占位，不借用其他模块图片。
- 点击已采用图片打开独立大图窗口，支持适应窗口、100%、放大和缩小。
- 总览只读取当前 `NoCadSchemeResult.visual_targets`，继续以稳定目标ID保持模块绑定。

Out of scope:

- 不把总览卡片合成为一张PPT图片。
- 不在总览中修改结构、提示词或采用状态。
- 不增加图片拖拽编辑、裁切、标注或局部重绘。
- 不自动生成缺失图片。

## Inputs And Outputs

Inputs:

- 当前方案名称、产品名称。
- 当前确定性视觉目标列表及各目标的 `targetId/title/imagePath`。

Outputs:

- 一个可滚动、自适应1～3列的大尺寸只读总览窗口。
- 一个按需加载原图的大图查看窗口。

## Behavior

- 正常路径：打开总览后，第一张为整机，其余按当前模块顺序排列；有图显示缩略图，点击后显示原图。
- 模块联动：增加、删除或排序模块后，再次打开或刷新总览时按当前视觉目标重建卡片，不保存第二份模块状态。
- 缺图：卡片保留并显示“尚未采用效果图”，不能点击打开其他图片。
- 无效路径/损坏图片：卡片显示“图片文件不可用”，大图入口禁用。
- 性能：总览只解码适合卡片尺寸的缩略图；只有用户点击时才读取原始分辨率图片。
- 兼容：支持 PNG、JPEG、BMP、WebP 等 Qt 可读取图片，并应用图片方向信息。

## Risk

- 超高分辨率原图在大图窗口中可能占用较多内存；通过仅点击时加载降低常态开销。
- 总览为审核工具，不改变正式PPT绑定和生成结果。

## Acceptance Tests

- 给定演示方案，打开总览后卡片数等于 `1 + 模块数`，顺序与视觉目标一致。
- 给定缺少采用图的目标，卡片显示缺图状态且不打开大图。
- 给定有效采用图，卡片使用缩略图且点击后打开对应目标的大图窗口。
- 增加或删除一个模块并刷新后，总览卡片数相应增减一个。
- 全量单元/界面测试和规定的Python编译检查通过。

## Implementation Notes

- 新界面放在独立 `ppt_generator/ui/module_visual_overview.py`，不把图片展示逻辑继续堆入无CAD业务服务。
- `NoCadSchemeEditor`只负责把当前结果传给总览窗口，不让总览直接修改 `EquipmentScene`。
- 使用 `QImageReader.setScaledSize()`生成缩略图，避免总览一次读取全部原始像素。
