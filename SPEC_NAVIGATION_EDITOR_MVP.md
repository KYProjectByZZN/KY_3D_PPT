# Spec: 顶部导航栏目编辑 MVP

> 状态：2026-08-24 已实现 0.7.11 全宽无阴影灰色底线与活动红线修正。

## Goal

把 PPT 顶部导航从固定模板文字升级为项目级结构化配置。默认栏目调整为“公司简介、工艺分析、设备设计、检测效果、系统介绍”，用户可在桌面软件中新增、修改、删除、排序栏目，并指定各栏目包含哪些 PPT 模块。

## Scope

In scope:

- 每个导航项保存栏目名称和对应的模板模块 key。
- 软件“导航样式”区域增加栏目摘要和“编辑栏目”入口。
- 编辑窗口支持新增、重命名、删除、上移、下移和分配模块。
- 默认提供 5 个栏目，允许 1～7 个栏目；名称不能为空、不能重复，单项不超过 10 个字符。
- 同一模板模块最多归属一个导航项；重新分配时从其它导航项自动移除。
- 项目保存、重新打开、当前页预览和最终 PPT 共用同一导航配置。
- 当前栏目背景、高度、自适应/手动加粗字体、深浅色对比和底部状态条带继续生效。

Out of scope:

- 在 PPT 画面内直接拖动导航文字或单独调整每个栏目的字体、宽度和颜色。
- 自动创建章节页、目录页或修改方案模块顺序。
- 根据 AI 语义自动判断自定义模块归属。

## Inputs And Outputs

Inputs:

- 导航栏目名称、顺序和模块归属。
- 当前项目的 `PresentationStyle`。
- 最终页面的 `template_module_key`。

Outputs:

- `*.kyppt.json` 中持久化的 `presentation_style.navigation_items`。
- 当前页预览和最终 PPT 顶部按项目配置绘制的导航栏。
- 当前页面所属栏目使用单格背景、强调文字和底部细线；未分配模块不显示活动栏目。

## Behavior

- 新项目和未保存导航配置的旧项目使用以下默认归类：
  - 公司简介：封面、公司资质、版本记录、结束页。
  - 工艺分析：需求分析、检测流程。
  - 设备设计：设备总览、设备模块、设备参数。
  - 检测效果：检测效果、检测项目。
  - 系统介绍：视觉系统、工控系统、AI算法、主要部件。
- 增加栏目时先创建空栏目，再由用户分配模块。
- 删除栏目后，其模块变为未分配，不自动猜测新的归属。
- 编辑窗口只有点击“确定”才写回项目；取消不改变当前项目。
- 模块分配以模板模块 key 为单位；复制出的同类型模块跟随同一导航栏目。
- “导航背景”在界面中明确为“当前栏目背景”：颜色只填充当前页面所属的一个栏目格，其它栏目保持白色背景。
- 字号默认选择“自动（随高度）”：`0.52 in`基准高度对应`10 pt`，导航变高时字号按比例增大并限制在`9～14 pt`；用户也可手动选择`9～16 pt`固定字号，手动值优先于自动计算。
- 导航不使用灰色阴影、栏目白色条带或逐栏目阴影；白色导航背景底部从幻灯片最左侧到最右侧绘制一条连续灰色底线，包含最右侧公司Logo区域，当前栏目在同一位置覆盖一段等高红线。
- 栏目可用区从幻灯片左边界`0 in`到Logo区前`11.55 in`无缝等分；第一栏不保留左缩进，相邻栏目的左右边界必须完全相接。
- 当前红线与所属栏目格使用同一左右边界，灰线和红线的高度、纵向位置完全相同，底边与整条导航背景底边重合；线条下方保持页面原背景，不创建任何阴影。
- 调整导航高度、当前栏目背景或栏目配置后，右上当前页展示先在已有页面图上绘制即时导航预览；Office实时图完成后再替换，Office失败也能看到导航尺寸和颜色变化。
- 导航即时画面显示期间不覆盖半透明“加载中”遮罩；状态文字直接显示当前高度和解析后的实际字号，让仅修改高度、白色背景不变时也能明确确认新值。
- Renderer 清空兼容模板布局中的旧固定导航文字和分隔线，再在每个生成页面上绘制当前项目的栏目、分隔线和活动下划线；不修改源模板。
- 旧项目缺少 `navigation_items` 时直接补默认值，不提升项目 schema 版本。

## Risk

- 栏目过多或名称过长会影响横向空间，因此首版限制为最多 7 项、每项最多 10 个字符。
- 模板布局结构不同可能无法识别顶部背景和 Logo；不兼容布局保持原样，不做模糊覆盖。
- 页面级导航对象会增加少量 Shape 数量，但只作用于生成副本，不影响源模板。

## Acceptance Tests

- 新项目默认显示五个新栏目，且 16 个现有模板模块均有确定归属。
- 导航项新增、改名、删除、排序和模块分配可以保存并重新读取。
- 空名称、重名、超过 7 项、超过 10 个字符和重复模块归属被拒绝。
- 软件编辑窗口取消不改变项目，确认后摘要、预览指纹和页面预览刷新。
- 调整高度或当前栏目背景后，不等待Office导图即可在当前页展示看到近似效果。
- 自动字号随高度增大；选择手动字号后，高度变化不覆盖所选字号；保存项目并重开后模式和字号保持一致。
- 真实 NAT6704 模板生成后，兼容页面显示五个新名称；设备、检测效果和系统页面分别高亮正确栏目。
- 当前栏目背景Shape和红色下划线的左右边界与该栏目文字格一致；非活动栏目不使用所选背景色。
- 页面不得生成`KY_NAV_INACTIVE_*`白色条带；导航背景及底线不得保留`a:outerShdw`外阴影。每个导航页生成一个覆盖幻灯片全宽`0～13.333 in`（含Logo区）的`KY_NAV_BASELINE`灰色底线，活动页再在栏目区生成一个`KY_NAV_ACTIVE`红线。
- 第一栏左边界为`0`，所有栏目格连续相接，最后一栏右边界为`11.55 in`；当前红线左右边界与对应栏目格完全一致。
- 快速预览必须先清除模板原始`0.892 in`导航背景和旧阴影，再绘制实色灰色底线与活动红线，不绘制渐变替代阴影；线条下方像素保持白色，避免旧灰影重新显示。
- 软件实际选择页面后，仅修改导航高度，快速预览PNG哈希和红条垂直位置必须立即变化，且即时画面上方不能出现加载遮罩。
- 输出 PPTX ZIP 结构有效、可由 `python-pptx` 重开，源模板 SHA-256 不变。
- 完整单元测试与规定的 Python 编译检查通过。

## Implementation Notes

- 数据模型：`ppt_generator/project.py` 的 `NavigationItem` 和 `PresentationStyle`。
- UI：`ppt_generator/ui/dialogs.py` 与 `ppt_generator/ui/main_window.py`。
- Renderer：`ppt_generator/navigation_style.py`；优先使用 `python-pptx` 页面 Shape API，不直接修改 PPTX ZIP/XML。
- 最终页与导航归属通过 `SlideContext.template_module_key` 连接。
