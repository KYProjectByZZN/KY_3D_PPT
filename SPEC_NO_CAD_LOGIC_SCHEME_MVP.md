# Spec：无 CAD 设备逻辑方案编辑器 MVP

## 1. 目标

在没有 CAD 的售前阶段，用标准设备模块和明确工艺主线生成“逻辑可审核、结构可调整、可用于技术方案沟通”的设备概念图。

本期优先保证：

- 上料、输送/定位、检测、分选、下料顺序合理；
- 工位和模块不会被 AI 随意增加、删除或交换；
- 用户可以替换模块、拖动列表排序、锁定已确认模块并重新排布；
- 逻辑错误明确阻断后续 AI 效果图生成；
- 结构图不追求接近真实设备外观，不宣称是 CAD 或制造图。

## 2. 范围

### 2.1 本期实现

- 独立的标准设备模块目录；
- 单条从左到右的产品主线；
- 模块添加、替换、删除、上移、下移和列表拖动排序；
- 模块名称、工位编号、说明、参考图路径和锁定状态编辑；
- 自动排布，锁定模块保留坐标；
- 输入、模块间连接和输出的自动重建；
- 阻断、警告、提示三级逻辑检查；
- 确定性 SVG 结构方案图；
- 确定性 Scene JSON、场景哈希和 AI 生成约束摘要；
- SVG 与 Scene JSON 导出；
- 通过主软件中的独立实验入口人工审核。

### 2.2 本期不实现

- 不调用 Codex、GPT Image、ComfyUI、ControlNet 或其他生图 Provider；
- 不输出 CAD、STEP、SolidWorks 或工程尺寸图；
- 不做自由 2D/3D 机械绘图；
- 不判断机构强度、节拍计算、气缸选型、机械干涉或可制造性；
- 不写入正式候选方案和 PPT；
- 不建立公司完整历史模块库，本期使用内置通用模块验证交互和逻辑；
- 不支持分叉、回流、并联工艺，多出口先封装为“OK/NG 收料”模块。

## 3. 模块边界与状态权威

- `no_cad_scheme.py`拥有模块目录、场景模型、操作服务、逻辑检查、SVG与AI约束摘要；
- `ui/no_cad_scheme_editor.py`只收集操作和展示服务结果；
- `EquipmentScene`是本功能唯一权威状态；
- UI不得直接修改其他项目模块、候选方案、`PptProject`或Renderer；
- 主软件只负责创建独立页签，不建立反向联动；
- 后续真正接入图片Provider时，只读取已通过检查的Scene快照和哈希。

## 4. 输入与输出

### 4.1 内置模块目录

首版覆盖：

- 上料：人工、振动盘、料盘、皮带；
- 输送：皮带、直线送料；
- 定位：阻挡定位、旋转定位；
- 检测：顶部、侧面、底部、多视角；
- 分选：气吹、气缸推料；
- 下料：OK/NG收料、皮带下料。

每个目录模块包含稳定类型ID、名称、类别、用途说明、内部已确认部件和结构图颜色。

### 4.2 EquipmentScene

场景至少保存：

- 场景版本、方案名称、产品名称；
- 有序流程模块；
- 每个模块的稳定节点ID、类型、名称、工位编号、说明；
- 每个模块的位置和大小；
- 锁定状态和可选参考图；
- 相邻模块间产品流连接。

### 4.3 输出

- SVG结构方案图；
- Scene JSON；
- 逻辑检查结果；
- `sceneHash`；
- 供后续图片Provider使用的生成约束摘要。

所有输出不包含随机数、当前时间或UUID。同一Scene必须产生相同哈希、SVG和约束摘要。

## 5. 逻辑规则

### 5.1 阻断级

- 模块少于2个或多于12个；
- 节点ID重复或引用未知模块类型；
- 第一模块不是上料；
- 上料模块不在第一位或存在多个上料；
- 最后一模块不是下料或下料出现在中间；
- 没有任何检测模块；
- 分选位于全部检测之前；
- 分选之后再次出现检测；
- 主线连接不是严格相邻、存在悬空节点或连接数量错误；
- 模块尺寸无效；
- 自动排布后产品主线方向不是从左到右。

存在阻断项时，结构图仍可用于定位问题，但`canGenerateAi`必须为`false`。

### 5.2 警告级

- 检测前没有明确定位模块；
- 检测后没有分选模块；
- 振动盘上料后直接进入检测，中间没有送料或定位；
- 工位编号为空或重复；
- 相邻模块在结构图上发生重叠。

### 5.3 提示级

- 模块未提供参考图；
- 已锁定模块在自动排布时保留原位置；
- 当前结构仅为售前逻辑示意，仍需工程师确认。

## 6. 界面行为

- 主入口名称改为“无CAD方案实验室”；
- 内部默认打开“逻辑方案编辑器”，保留原“提示词/SVG实验”；
- 左侧显示标准模块库和有序设备主线；
- 主线支持按钮和列表拖动调整顺序；
- 中间显示结构SVG；
- 右侧编辑当前模块属性，并查看逻辑检查、Scene JSON和AI约束；
- 每次结构或属性变化后自动重建连接、检查逻辑和刷新结构图；
- 存在阻断项时显示红色状态；通过时显示绿色状态；
- “生成AI效果图”按钮本期保持禁用，并说明是Provider未配置或逻辑未通过；
- 导出由用户主动触发，不自动覆盖正式方案文件。

## 7. 风险

| 风险 | 控制 |
| --- | --- |
| 用户把概念图当制造图 | 界面和SVG明确标注“无CAD逻辑方案 / 非制造图” |
| AI后续改变工位逻辑 | 图片Provider只能读取通过检查的Scene快照与哈希 |
| 规则过度限制真实设备 | 本期只支持单主线并明确边界，警告不全部阻断 |
| 标准模块和真实公司结构不符 | 内置目录仅用于功能核验，后续由用户提供公司模块逐项替换 |
| 锁定模块导致顺序与坐标冲突 | 保留坐标并产生阻断或重叠警告，禁止静默移动 |

## 8. 验收标准

1. 内置演示场景逻辑通过，模块顺序为上料→送料→定位→顶部检测→侧面检测→分选→OK/NG下料；
2. 删除全部检测、把分选移到检测前、把上料移出首位时分别产生阻断项；
3. 添加、替换、删除和调整顺序后连接自动恢复为相邻单主线；
4. 自动排布使未锁定模块从左到右排列，锁定模块坐标保持不变；
5. 同一场景重复执行得到完全相同的Scene哈希、SVG和AI约束；
6. SVG可被XML解析，模块节点数量与Scene一致；
7. UI可离屏创建、显示演示场景、编辑模块并刷新检查；
8. AI按钮保持禁用，原方案图实验功能仍可打开；
9. 原有完整测试继续通过；
10. 生成界面截图、SVG和Scene JSON供用户审核。

## 9. 后续确认后再做

- 用公司真实历史设备图片替换通用模块；
- 增加公司模块的输入/输出接口和适用产品规则；
- 增加自由画布拖动、缩放和局部遮罩；
- 接入GPT Image或ComfyUI/ControlNet；
- 实现同Scene多候选图、锁定区域局部重绘和图片Provider切换；
- 把人工确认图写入正式候选方案和PPT。
