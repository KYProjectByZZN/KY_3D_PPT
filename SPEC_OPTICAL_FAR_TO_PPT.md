# Spec: 光学 FAR 一键生成检测效果

> 状态：2026-08-23 已完成 0.6.3 的OK页文字语义修订。首个真实样例为 `templates/光学资料/NAT6801FAR(8.5).xlsx`，PPT 模板为 NAT6704 23页模板；Microsoft PowerPoint 已打开最终24页成品，WPS因本机未安装待实机验收。

## Goal

在桌面软件的 Excel 工作台增加“一键生成检测效果”。导入统一格式的光学 FAR 后，自动读取检测项标准、工位相机/镜头/光源、生产节拍和检测图，生成可继续人工编辑的 PPT 项目模块。

## Scope

In scope:

- 参考 DCE `core/excel_parser.py` 对版式化 FAR 的识别规则，在 PPT 项目内实现独立、可测试的轻量解析器，不依赖 DCE 运行目录。
- 读取右上角“检测项汇总/验收标准”。
- 读取“检测项分析”中的工位视角、配置、检测项、嵌入图片和图片说明。
- 读取检测速度和特别说明。
- 有对应缺陷图的检测项各使用 PPT 第11页单图版式生成1页；同一工位没有对应缺陷图的检测项合并为1张OK页，图片数量不决定页数。
- 替换“检测效果”模块已有页面，不重复叠加旧导入结果。
- 更新“产品检测项”表格与“设备参数”中的生产节拍、工业相机、工业镜头、工业光源。
- 提取图片保存到 `output/far_assets/<FAR_SHA256>/`，项目和最终 PPT 引用稳定文件路径。

Out of scope:

- 自动判断光学方案是否合理或自动承诺检测率。
- 修改 FAR 原文件。
- 自动生成新的 PPT 版式或在一页中重新排列多张图。
- 支持与当前版式完全无关的任意 Excel。

## Inputs And Outputs

Inputs:

- 用户在 Excel 工作台选择的 `.xlsx` 光学 FAR。
- 当前 `PptProject`、NAT6704 模板及 manifest。

Outputs:

- 当前项目的检测效果页、产品检测项表和设备参数被更新。
- 提取的检测图素材和解析摘要。
- 用户再点击“生成 PPT”时得到完整、可编辑 `.pptx`。

## Behavior

- 正常路径：选择 FAR → 点击一键生成 → 解析验收标准和工位 → 提取图片 → 替换三个目标模块的结构化数据 → 界面刷新并显示页数摘要。
- 检测效果页：同一工位内的检测项先去重；有对应缺陷图的检测项各生成1页，每页只放该缺陷的第一张匹配图。
- OK合并页：同一工位没有对应缺陷图的一个或多个检测项全部合并到1页，逐项写明，并只放该工位第一张 `OK` 图；不为每个无图检测项重复生成OK页。
- OK页文字：明确写成“OK样件图（关联检测项）”，括号内列出该页关联的检测项；不得把OK图表述成这些缺陷的成像图。底部同时说明该工位没有其它对应缺陷样图。
- 混合工位：部分检测项有缺陷图、部分没有时，有图项各自1页，无图项共同1张OK页；没有OK图时明确报错，不跨工位借图。
- 工位标注：页面固定显示“工位1：俯视”这类“工位编号：工位视角”，标题、结构化页面名称和说明保持一致。
- 产品检测项：表头保留，行数固定为模板的9行；最多写8条，不足补空行，超过时明确报错，不静默丢数据。
- 检测状态：`√` 为“是”，`⌂` 为“是”并在项目名中标注 AI，`?` 为“待评估”，`X` 为“否”。
- 设备参数：生产节拍只取 Excel 的“检测速度”；相机、镜头、光源从各工位配置聚合；未要求更新的其他模板参数保持原值。
- 重复导入同一 FAR：按“工位＋缺陷检测项页/OK合并页”重建检测效果模块，复用同一图片缓存目录，只登记实际选用图片，不重复生成页面。
- 缺少“检测项汇总”、“检测项分析”、工位配置或检测图时，显示具体缺失内容，不修改当前项目。

## Risk

- FAR 嵌入图片和合并单元格较复杂；按 DCE 已验证的“表头文字+图片锚点行列”规则解析，并用真实 FAR 回归。
- 同一检测项可能有多张缺陷图；当前版本按 FAR 顺序选第一张，用户可在结构化页面中人工替换。
- 检测项与图片说明可能不完全一致；采用“完全相同或一方包含另一方”的保守匹配，不做模糊语义推断。
- 生成结果仍需人工确认样件、限度标准和客户验收口径；软件不自动承诺检出率。

## Acceptance Tests

- 给定 NAT6801 FAR，解析得到5条检测项、3个工位和21张检测图。
- 读取生产节拍 `40-50pcs/min`，聚合出500万/1200万相机、FA/远心镜头、同轴光/背光。
- 应用后检测效果模块为6页：工位1的四个无图检测项合并为1张OK页，工位2四个有图检测项各1页，工位3翘曲1页；完整PPT为24页。
- 工位1的OK页逐项写明指纹、镀层不良、生锈、磨伤，只展示1张工位1 OK图；工位2和工位3使用各检测项的第一张匹配图。
- 工位1主文字为“OK样件图（指纹、镀层不良、生锈、磨伤）”的两行排版，语义是检测项关联，不表示图中存在这些缺陷。
- 每页显示对应的“工位N：视角”，检测项和图片文件存在；重复应用后仍为6页。
- 产品检测项表为9×5，翘曲、指纹、镀层不良、生锈、磨伤及标准与 FAR 一致。
- 设备参数7×4表只更新指定四项，其他单元格保持模板原值。
- 真实生成 PPTX 能被 `python-pptx` 重开，ZIP 必需项存在，原模板 SHA-256 不变。
- Windows 原生界面能看到按钮和生成后的模块页数；Microsoft PowerPoint 实机打开/预览不报修复。

## Implementation Notes

- 新增 `ppt_generator/optical_far.py`：数据模型、FAR 解析、设备摘要、图片持久化和 `PptProject` 映射。
- 扩充 `templates/NAT6704_v2.template.json` 的第11页 Slot，不修改 PPTX 模板。
- `MainWindow` 只负责选文件、调用核心服务、刷新界面和显示摘要。
- 渲染继续使用现有 manifest Slot 和模块页复制链路，不增加专用 PPT XML 写入分支。

## Validation Result

- NAT6801 实测解析：5条检测项、3个工位、21张检测图。
- 0.6.0 的39页输出属于旧的“每图一页”规则，仅保留为历史验证文件，不再作为当前验收结果。
- 0.6.1 最终输出：`output/NAT6801_FAR_OneClick_v0.6.1_Final.pptx`，27页，其中检测效果9页。
- 图片选择：工位1四页共用OK图；工位2四页和工位3一页使用检测项首张匹配图，共引用6张唯一图片。
- 自动验证：34项测试通过；重复工位检测项去重、重复应用、27页界面统计、6条素材、节拍和表格均通过。
- 文件验证：ZIP无损坏，`python-pptx`重开正常，原模板哈希不变；Microsoft PowerPoint 实际打开无修复提示。
- 0.6.3最终输出：`output/NAT6801_FAR_OneClick_v0.6.3_Final.pptx`，页数和图片规则与0.6.2相同；工位1改为“OK样件图（关联检测项）”并明确无其它缺陷样图。
- 0.6.3自动与文件验证：34项测试通过，ZIP、24页重开、模板哈希和Microsoft PowerPoint打开均通过。
- 0.6.1属于过渡版；0.6.2完成后以“无图项合并OK页”的24页结果作为当前验收文件。
- 0.6.2最终输出：`output/NAT6801_FAR_OneClick_v0.6.2_Final.pptx`，24页，其中检测效果6页。
- 工位1四个无图检测项合并在1页并只引用1张OK图；工位2四项和工位3翘曲各引用第一张匹配缺陷图，共6张唯一图片。
- 自动验证：34项测试通过；重复检测项去重、重复应用、24页界面统计、6条素材、节拍和表格均通过。
- 文件验证：ZIP无损坏，`python-pptx`重开正常，原模板哈希不变；Microsoft PowerPoint 实际打开无修复提示。
