# Spec：方案图实验室 MVP（独立核验版）

## 1. 目标

在不改动“自动方案 v2”正式需求、候选方案、PPT 项目和模板的前提下，增加一个完全独立的“方案图实验室”，用于先核验方案图生成链路中的三件事：

1. 结构化 `DrawingSpecification` 能否稳定转换成可检查的设备布局；
2. 相同输入能否得到完全相同的 `LayoutPlan`、SVG 结构示意图和 `PromptRecipe`；
3. 工程师能否直接修改 JSON、重新生成、预览并导出结果。

本阶段验证的是“可控输入与稳定中间产物”，不是验证某个 AI 绘图模型的美术能力。

## 2. 范围

### 2.1 本期实现

- 独立编辑和校验 `DrawingSpecification` JSON；
- 单条水平主线、1～8 个工位的确定性布局；
- 生成 `LayoutPlan` JSON；
- 生成可缩放、可导出的工业结构示意 SVG；
- 生成 Provider 无关的 `PromptRecipe`、正向提示词、负向提示词和固定种子池；
- 生成方案图核验清单；
- 在 PySide6 中即时展示 SVG 和以上结构化结果；
- 提供演示数据、SVG 导出和 PromptRecipe 导出；
- 在主软件中只增加一个独立入口，不读写正式候选方案。

### 2.2 本期不实现

- 不调用 OpenAI、ComfyUI、ControlNet、Stable Diffusion 或其他图片 API；
- 不把实验结果写入“自动方案 v2”候选方案；
- 不写入 PPT 项目、不替换 PPT 图片、不修改模板；
- 不生成 CAD、机械施工图或可制造性结论；
- 不根据工位名称擅自增加相机、镜头、光源、机械臂或旋转机构；
- 不承诺 SVG 是最终设备外观图。

## 3. 输入

输入沿用现有 `DrawingSpecification` 数据结构：

- `drawingType`：图纸用途；
- `product`：产品名称、类型、型号、尺寸、材料；
- `overallLayout`：总体布局描述；
- `processFlow`：工艺步骤；
- `stations`：工位数组；
- `motionRelations`：运动关系；
- `keyStructures`：关键结构；
- `annotations`：补充标注；
- `prohibitedElements`：禁止出现内容；
- `referenceImages`：参考图片路径。

每个工位至少包含唯一的 `stationId` 和非空 `name`。可选字段为：

- `description`；
- `position`；
- `fixedParts`；
- `movingParts`；
- `visionParts`；
- `fixture`。

只有输入明确给出 `visionParts` 或 `fixture` 时，示意图和提示词才允许出现对应内容。

## 4. 输出

### 4.1 LayoutPlan

- 固定画布：1600 × 900；
- 固定边距、设备主线和工位区域；
- 工位严格保持输入顺序；
- 每个工位具有稳定坐标和尺寸；
- 输入、工位间流向和输出均有明确连接；
- 包含输入规范 SHA-256 和布局 SHA-256；
- 不包含随机数、当前时间、UUID 等不稳定字段。

### 4.2 SVG 结构示意图

- 输出合法 SVG XML；
- 画面明确标注“结构示意图 / 非 CAD 施工图”；
- 采用工业灰、深灰、KY 红的扁平视觉；
- 工位数量、编号、名称、顺序必须与输入一致；
- 明确显示从左到右的输入、主输送方向和输出；
- 仅显示输入中明确确认的视觉部件和治具；
- 每个工位包含稳定的 `data-station-id`，便于自动核验。

### 4.3 PromptRecipe

- `provider` 固定为 `unconfigured`；
- 固定工作流版本、提示词模板版本、画布比例、种子池和控制参数；
- 正向提示词明确工位数量、顺序、产品流向和已确认部件；
- 负向提示词包含用户禁止项，并禁止擅自增加未确认结构；
- 明确要求后续生图以 SVG/线稿为结构控制输入；
- 包含布局 SHA-256，便于后续追踪同一布局的不同 AI 结果。

## 5. 界面行为

- 主软件增加“方案图实验室（试验）”页签；
- 页面顶部始终显示“独立实验，不读取/修改正式候选方案，不写 PPT”；
- 左侧编辑 JSON，可加载演示数据和重新生成；
- 中间展示 SVG；
- 右侧查看 LayoutPlan、PromptRecipe、最终提示词和核验清单；
- AI 候选图按钮保持禁用，并明确显示“未配置”；
- JSON 无效时只显示错误状态，不弹出阻塞式对话框；
- 导出时由用户主动选择文件，不自动覆盖既有文件。

## 6. 风险与控制

| 风险 | 控制方式 |
| --- | --- |
| AI 每次生成结果不同 | 本期先冻结确定性的规格、布局、SVG、Recipe 和种子池 |
| 模型擅自补充结构 | 只把显式部件写入布局，负向提示词禁止未确认部件 |
| 把概念图误当工程图 | 界面和 SVG 双重标注“结构示意 / 非 CAD 施工图” |
| 实验数据污染正式方案 | 独立服务和独立界面，不注入正式 Application/Repository |
| 修改一个模块影响 PPT | 本期唯一集成点是主窗口页签，不连接渲染器和 PPT 项目 |

## 7. 验收标准

1. 同一输入连续生成两次，`LayoutPlan` 字典、SVG 字节、`PromptRecipe` 字典和哈希完全一致；
2. 修改工位顺序后，布局哈希和 SVG 发生变化，显示顺序与新输入一致；
3. 1～8 个合法工位可生成，空工位、超过 8 个工位、重复工位 ID 被拒绝；
4. 未填写 `visionParts` 时，输出不得出现相机、镜头、光源；填写后才显示；
5. SVG 可由 XML 解析器解析，且 `data-station-id` 数量与输入工位数一致；
6. 独立界面离屏测试可创建、生成并显示结果，AI 按钮保持禁用；
7. 原有完整测试继续通过；
8. 生成一套可供人工审核的 SVG、PromptRecipe JSON 和界面截图。

## 8. 后续确认点（不在本期开发）

用户审核本实验功能后，再决定：

- 是否把确认过的 DrawingSpecification 从“自动方案 v2”导入实验室；
- 是否增加“回写为候选方案图”；
- 选择 ComfyUI/ControlNet 本地工作流，或商业图片 API；
- 建立同一 Recipe 多种子候选、机器核验和人工确认流程；
- 最终确认图如何进入设备介绍模块和 PPT 图片槽位。
